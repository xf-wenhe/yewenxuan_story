# V5（ch551–750）对话引号修复计划

> 2026-09-24 摸底后定稿。承接 `tools/fix_r251_quote_restore.py`（阶段 1 试点已 apply 于 ch745/650/654，未提交）。
> 本文件是**工作计划**，不含正文改动。开工前每一步的验收口径见第四节。

## 一、现状：四条实测事实

1. **引号被剥是损坏，不是风格。** `9252b142`（2026-08-22「V5 mechanical deslop complete」）一次把 V5 的 ASCII 引号从 182,077 削到 2,747（−98.5%），提交信息自己写着 `Quote-striipping`。200 章里 196 章受影响、111 章归零。

2. **父本不能整卷回退。** `9252b142^` 的 90,191 个引号对里，只有 20,206 对（22.4%）是对话，其余 69,985 对（77.6%）是术语/强调引号（`感觉到`/`编译`/`结构`/`沉睡`）——正是 R238 从 V6/V7 清掉的那类 AI 味。整卷回退 = 撤销 R238。

3. **父本自身分两类，必须分开对待。**

   | 类 | 判据 | 章数 | 章号 |
   |---|---|---|---|
   | 坏父本 | 每行引号 > 10 | **46** | 665、666、668–707（缺 667）、711、719、720、721 |
   | 可用父本 | 每行引号 ≤ 10 | 154 | 其余 |

   坏父本带的原始形态是**强调引号灾难**，每个短语都被引号包住，句号被塞进引号里：

   ```
   父  叶文轩能"感觉到"0428备份在"发光"，0428备份在"说"：开始。"吧"。
   父  白色空间在。"变"。0428备份的"粉色纹路"在"变亮"，是"逐渐。"的"。"像"一个"灯"在。"慢慢"。"调亮"。
   现  白色空间在变0428备份的粉色纹路在变亮，是逐渐，的一个灯在慢慢调亮。
   ```

   现值坏在**两层**：引号被剥 + `。"` 被换成 `，`（族 B）。这 46 章的父本不能当机械对齐的参考，但**仍是"原文想说什么"的唯一见证**。

4. **坏的边界不止这 46 章。** 全卷「引号之外的引述信号」（`说：` 与紧邻引述结构）共 **3,842 处 / 199 章**，分布 30+ 有 39 章、15–29 有 70 章、5–14 有 71 章、1–4 有 19 章、0 有 1 章——这是工作量的**下界**（不含无引述的短台词如 `"嗯。"`）。

### 一·补（2026-09-24，R251 提交后实测，**推翻上面第 4 条的排批依据**）

5. **机械通道已经见底。** 对齐行上父本共带 **11,425 对**引号，`is_dialogue` 只认 **2,465 对（21.6%）**——与全卷「父本对话占 22.4%」吻合，说明分类器是准的，是父本里 78% 本来就是术语/强调引号。R251 已拿走 **2,286 对（92.7%）**。剩下的 8,960 对里，真正属于「分句台词上半」（内容以 `，` 收尾且紧跟引述结构，如 `"大嘴，"叶文轩再次开口，…`）的只有 **40 对 / 19 章**。**放宽闸门③（行内已含引号则跳过）净增 0 对**——实测那 2,429 对本来就在（2,286 是 R251 补的，143 是原文就有）。即：再把 R251 调松也榨不出东西了。

6. **父本只覆盖 23% 的行，对它 77% 的正文不是「原文」而是「见证」。** 全卷 43,787 行父本里只有 10,115 行能与现值对齐。`ch629` 是标本：现值 120 行 vs 父本 **681 行**，**对齐 0 行**；因为该章被后续轮次整章重写过（同类的 `b40b715b`＝R213 对 ch665 一次换了 276 行，把 deslop 的逗号碎片重写成通顺散文、台词改用 `「」`）。**凡是重写过的章，R251 那种「照父本重放」在原理上就不可能命中。**

7. **R252 的真实病灶是两类，且都不能照父本回填。**

   - **(甲) 有引述结构、却一个引号都没有的行。** 粗测 **954 行 / 180 章**（含 `大嘴没有回答`、`不能现在问` 这类假阳性，真数约一半）。标本 ch551：`大嘴，叶文轩再次开口，转身走回去。"你胸口那块碎片。它怎么了？"`——**半行有引号、半行没有**（父本两半都有）。这类是读者一眼能看见的伤。
   - **(乙) 被机械剥走的标点。** 剥引号那轮的顺序是**先 `。"` → `，`、再删引号**，所以现值的 `，` 正是那两轮的残渣。**可证**位置 **1,442 处 / 119 章**（父本 `。"` ↔ 现值 `，`，重灾 ch683 95 / ch682 50 / ch587 39 / ch631 38）。另有 1,823 处父本有 `。` 而现值是 `，` 的差异**不可证**——多为后续轮次**有意**的逗号链，**勿动**。
   - ⚠️ **两类都不能信父本。** 见 ch556 L54：父本自己写作 `石材中有维护派预置的。"记忆提取模块"。`——**句号被塞进了引号里**，父本也是坏的。正解是读句子定夺（此处应删逗号：`预置的记忆提取模块。`）。**父本给的是「原文想说什么」，不是「原文是什么」。**

8. **「坏父本 46 章」这个标签对排工作量没有意义。** 它衡量的是父本自己的强调引号密度，与现值还缺多少引号无关——所以第 4 条按「引述信号」排批也要降级。按第 7 条实测，缺陷密度 top20 是：683、682、587、631、635、705、714、570、568、633、691、695、634、654、692、632、652、690、588、591——**横跨 551–750，不是集中在最坏带**。故 R252 改按**自然序**分批（下表不变，理由改掉）。

9. **`「」` 不是台词，是标签。** V5 现值 `「` 只 440 个、140/200 章为 0；它是 R213 那类轮次给**模块名/显示文本**加的（`「锚点B抑制协议·执行层」`、`「三个结局，请选择」`）。**台词一律用 ASCII `"`，不要改用 `「」`**（V4 的 2,321 个 `「」` 是 V4 自己的事）。

## 二、目标口径

| 指标 | 现值 | 目标 |
|---|---|---|
| V5 CJK | 701,166 | 不变（引号与标点非 CJK） |
| ASCII 引号数 | 2,901 | 14,023–19,632 |
| 密度 | **4.1/千** | **20–28/千** |
| 对话引号对数 | 1,450 | 7,011–9,816 |
| 术语/强调引号对 | ~0 | 保持 ~0 |

参照带：V1 21.2、V2 22.1、V3 27.7、V6 24.3（V4 11.9 也偏低，是另一轮的事；V7 57.4 偏高）。

## 三、方法（三段）

### R251 阶段 1 —— 机械还原（全卷，一次提交）

`tools/fix_r251_quote_restore.py` 现有规则（只认精确对齐 `P == C` + 一对一映射 + 逐行自查）在**全卷**可还原 **1,751 对**（父本对话的 8.7%），把 V5 从 4.1 拉到 **9.1/千**。工具 fail-closed：`suspects > 0` 时拒绝写盘。

这一步单独做**不值得当终点**（会造出「40% 的章半有引号、60% 的章没有」），但作为**统一基线**值得——它严格更接近目标，且是纯机械、可审计、可重放。

### R251b —— 试验（只针对 46 章坏父本带，有 go/no-go）

阶段 1 的现行规则有**两个已知漏洞**，修掉它们可能把坏父本带从「全人工」变成「半自动」：

- **判据漏洞**：`is_dialogue` 现在把「内容含句号且 ≤40 字」一律当对话，于是 `"逐渐。"`、`"纹路。"`、`"的。"` 全被判成对话。更好的判据是**行位置**——整行（近乎）就是引号内容、或有紧邻引述、或长于 12 字，才算对话。
- **对齐漏洞**：`。"` → `，` 那批行 `P != C` 被直接跳过，但它们其实**只差标点**。把 `，。：！？、` 归一后比对（`norm(P) == norm(C)`），ch680 那类行就能对上。

**go/no-go**：在 ch665–707 上跑 dry，若该带还原率从 <2% 提到 ≥25%，则并入阶段 1 全卷跑；否则该带直接进阶段 2 全人工。**这是代码变更，动工前必过 legacy impact audit。**

### R252 阶段 2 —— 逐章补完（10 批）

逐章对照「父本（去引号）」与「现值」，只做两类编辑：

1. **加对话引号**；
2. **修复被剥离吃掉的标点**（`，中` / `，区域` 这类把逗号还原成句号或删掉）。

**硬约束：不回填父本被有意删掉的词。** 隐喻简化、`目光→视线`、`愣→怔`、`——→，` 都是既定简化（R238/V5 deslop 有意为之），回填等于撤销那些轮次。本阶段只补**引号与标点**。

每章产出一张**改动清单**（行号 / 修前 / 修后 / 依据：父本行 or 无父本依据），存 `.claude/tmp/`，供作者抽查。

## 四、分批表

| 批 | 章号 | 章数 | 说明 |
|---|---|---|---|
| R251 阶段 1 | 551–750 | 200 | 机械，一次提交；试点 3 章并入 |
| B1 | 665–684 | 20 | 坏父本带 |
| B2 | 685–704 | 20 | 坏父本带 |
| B3 | 705–724 | 20 | 坏父本 705–707、711、719–721（7 章）+ 常规 13 章 |
| B4 | 551–570 | 20 | |
| B5 | 571–590 | 20 | |
| B6 | 591–610 | 20 | |
| B7 | 611–630 | 20 | |
| B8 | 631–650 | 20 | 含试点 ch650 |
| B9 | 651–664 + 725–730 | 20 | 含试点 ch654 |
| B10 | 731–750 | 20 | 含试点 ch745 |

**按自然序扫（551 → 750），先打上表 B1–B3 只是沿用编号。** 原定「先打 665–724 最坏带」的理由（第 4 条）已被一·补第 8 条推翻：缺陷密度横跨全卷，最坏带并不更重。自然序的好处是**章内上下文连续**（同一批里人物状态、伏笔位置前后接得上），读句子定夺时误判率更低。

## 五、每批验收口径

一批 = 20 章，全部满足才算过：

1. **引号配对**：该批每章 `"` 计数为偶数；无 `""`；无跨行未闭合引号。
2. **无强调引号**：该批每章所有新增引号对通过 `is_dialogue` 判定，被误判的强调引号对 = 0。
3. **无错位引号**：无「引号插在半句中间」（R251 的 self-check + suspects 计数 = 0）。
4. **CJK 不动**：该批每章 CJK 计数与修前**逐章相等**（引号与标点非 CJK）。
5. **字数硬线**：该批每章 ≥3000 CJK。
6. **AI 味不涨**：该批每章 `polish_pipeline.py` 的 L1/DEADLY 命中数不增长。
7. **密度到位**：该批每章引号密度落在 **18–28/千**，V5 整体向 20–28 收。
8. **字节可重放**：`--verify` 从 `git show HEAD:<file>` 的字节重放该批全部编辑，**逐字节相同**（仅允许 EOL 差异——这是唯一能同时证明「只有预期处变了」与「CRLF/LF、无 BOM 未被扰动」的检查）。
9. **作者抽读**：每批抽 2–3 章通读，确认台词读起来是台词。

## 六、交付、回滚、进度登记

- **一批一提交**，英文里程碑信息 + 归属行，例如 `R252 B1: V5 dialogue quotes ch665-684 (N pairs, +N marks)`。
- **回滚 = `git revert` 该批那一个提交**，不影响其他批。
- 每批完成后在本文件底部追加一行进度（批号 / 章数 / 新增对数 / 该批密度 / 验收结果）。
- 全轮结束后复查：V5 密度、奇数引号文件数、术语引号对数、CJK 总量、L1/DEADLY 全库增量。

## 七、待作者拍板

1. **R251b 试验做不做？** 建议做——上限是把 46 章带从全人工变半自动；下限是白跑一次 dry（不写盘）。但它是代码变更，要走审计。
2. **试点 3 章（ch745/650/654）怎么处理？** 建议并入 R251 阶段 1 那一次提交——工具对已修行是跳过的（幂等），三章状态不会被二次改动。

## 八、工作量实话（2026-09-24 实测修订）

R251 阶段 1 + 1b 已把**机械能做的部分做完**（2,286 对，V5 从 3.9 → **10.6/千**）。一·补第 5 条证明再调松规则净增 0 对——**剩下的全是读句子才能判的**，这部分没有捷径。

- **已知缺陷量**：(甲) 引述旁无引号的行 ~954 处（真数约半）/ 180 章；(乙) 可证标点残渣 1,442 处 / 119 章。两项相加 ≈ **1,900–2,400 个待修位置**，摊到 200 章约 **每章 10–12 处**。
- **折算密度**：若这批全部落实，V5 约到 **13–15/千**；要到 20/千 还需要**「无引述结构的纯台词」**（`"嗯。"`、`"走吧。"` 这类，检测器数不到），那部分只能靠通读。所以**第 2 条的目标带 20–28/千 对 V5 未必够得着**，最终落点以通读结果为准，不要为凑指标硬加引号。
- **本轮性质**：R238 是**删**，可用脚本判定；这一轮是**补**，补在哪儿只有读得懂上下文才知道，且**父本 77% 的行对不上**（一·补第 6 条）。10 批、一批一次交付 + 抽读，是全书迄今最大的一轮。
- **不要做的事**：不要为凑密度给强调/术语加引号（那是撤销 R238）；不要回填父本被有意删掉的词（隐喻简化、`目光→视线`、`愣→怔`、`——→，` 都是既定简化）；不要动那 1,823 处**不可证**的 `。`/`，` 差异（多为有意逗号链）。

## 九、进度登记

| 批次 | 章号 | 改动行 | 新增引号对 | 该批密度 | 验收 |
|---|---|---:|---:|---:|---|
| R251 阶段 1 | 551–750 | — | 2,286 | 10.6/千 | 已提交 `b04b2416` |
| **B1a（自然序 1；表内 B4 的标点半批）** | 551–570 | 118（15 章） | 13 | 0.7–13.9/千 | 1/4/5/6/8 过，**7 未达**（见下） |
| **B1b（自然序 1；(甲) 类补引号）** | 551–570 | 69（17 章） | 112 | 6.3 → 9.3/千 | 1/2/3/4/5/6/8 过，**7 未达**（见下） |
| **B2a（自然序 2；标点半批）** | 571–590 | 83（14 章） | 4 | 9.2 → 9.3/千 | 1/2/3/4/5/6/8 过，**7 未达**（见下） |
| **B2b（自然序 2；(甲) 类补引号）** | 571–590 | 96（18 章） | 122 | 9.3 → 12.6/千 | 1/2/3/4/5/6/8 过，**7 未达**（见下） |
| **B3a（自然序 3；标点半批）** | 591–610 | 159（19 章） | 4 | 5.3 → 5.4/千 | 1/2/3/4/5/6/8 过，**7 未达**（见下） |
| **B3b（自然序 3；(甲) 类补引号）** | 591–610 | 114（20 章） | 203 | 5.4 → 11.2/千 | 1/2/3/4/5/6/8 过，**7 未达**（见下） |
| **B4a（自然序 4；标点半批）** | 611–630 | 29（7 章） | 1 | 6.0 → 6.1/千 | 1/2/3/4/5/6/8 过，**7 未达**（见下） |
| **B4b（自然序 4；(甲) 类补引号）** | 611–630 | 69（17 章） | 100 | 6.1 → 9.3/千 | 1/2/3/4/5/6/8 过，**7 未达**（见下） |
| **B5a（自然序 5；标点半批）** | 631–650 | 234（19 章） | 7 | 9.3 → 9.6/千 | 1/2/3/4/5/6/8 过，**7 未达**（见下） |
| **B5b（自然序 5；(甲) 类补引号）** | 631–650 | 22（8 章） | 35 | 9.6 → 10.4/千 | 1/2/3/4/5/6/8 过，**7 未达**（见下） |
| **B6a（自然序 6；标点半批）** | 651–664 + 725–730 | 91（11 章） | 3 | 6.2 → 6.3/千 | 1/2/3/4/5/6/8 过，**7 未达**（见下） |
| **B6b（自然序 6；(甲) 类补引号）** | 651–664 + 725–730 | 43（10 章） | 76 | 6.3 → 8.5/千 | 1/2/3/4/5/6/8 过，**7 未达**（见下） |
| **B7a（自然序 7；表内 B10 的标点半批）** | 731–750 | 36（10 章） | 8 | 25.4 → 25.6/千 | 1/2/3/4/5/6/8 过，**7 未达**（见下） |
| **B7b（自然序 7；(甲) 类补引号）** | 731–750 | 194（19 章） | 287 | 25.6 → 32.2/千 | 1/2/3/4/5/6/8 过，**7 未达**（见下） |
| **BP1a（自然序 8；表内 B1 的标点半批）** | 665–684 | 78（13 章） | 1 | 4.18 → 4.19/千 | 1/2/3/4/6/8 过，**5 未达**（ch675 2998，既存）、**7 未达**（见下） |

**B1a（2026-09-24，提交 `5f6d860f`）** —— 三处必须记下的判读：

1. **拆成 a/b 两半。** 计划 §四 把 B4 当成一个整批，但 §五 第 7 条（密度 18–28/千）**纯靠标点够不着**：本批 118 行只新增 13 对引号。故 B4 拆为 **B1a＝标点修复 + 整行台词**（已提交）与 **B1b＝(甲) 类补引号**（待做，需逐行读上下文）。**B4 作为整批尚未过验收**，第 7、9 条要等 B1b。

2. **(乙) 规则的完整形态已被反证并补全。** 父本 `A。"X"。B` → 现值 `A，XB`，逆变换是**删逗号 + 按父本补句号**。三个反例已单列手工行：`被。"上级"。"终止"了。`（句号收的是相邻强调引号链，不是句子）不补句号；`东西，。"保护欲"。`（逗号是同位语标记）保留逗号；ch563 L111 父本 `。"` 是**真实的叙述/台词分界**，逆变换会把叙述粘进台词，故整行手工。

3. **丁类已量化并留档，未动。** 本批范围内，与父本仍有差异但**规则按计划不许碰**的位点共 **88 处**（父本 `灰色的制服。灰色的脸。空白的编号牌。` → 现值 `灰色的制服，灰色的脸空白的编号牌。` 这类**有意逗号链**，§八 明令勿动），另有 8 行因长度不等未纳入比对。明细存 `.claude/tmp/r252_b1_ding.txt`，供作者判断是否另立一轮。规则**有意处理**的位点是 147 处（父本自身的强调引号灾难被摊平），两者已分开统计。

**工具：** `tools/fix_r252_b1a.py`（apply/verify/dry，整行锚点、唯一命中、fail-closed、幂等）+ `tools/r252_b1_sites.json`（118 行改动清单，含 `core_before`/`core_after` 供审读）。`--verify` 从 HEAD 的 blob 重放全表并要求逐字复现工作区，且校验行尾约定与锚点一致 —— 这是同时证明「只有预期处变了」与「CRLF/LF、无 BOM 未被扰动」的唯一检查。

**勘误一条：** 规划器最初从 `git show HEAD:` 取锚点，但 V5 工作区是 CRLF、blob 是 LF（`.gitattributes eol=lf` 提交时归一化），锚点永远搜不到。锚点必须按**工作区字节**建，比对必须在 **LF 空间**做。

**B1b（2026-09-24，(甲) 类补引号）** —— 69 行 / 17 章 / **新增 112 对引号**。最重的是 ch567：全章 113 行、改动前只有 2 个引号，本批 16 行 / 26 对。本批**不新增任何字数**，只补引号与剥掉的标点。

**判据（四条轴）：**

1. **补引号**：行内**有人名 + 言语动词**（`X说/问/答/开口/打断…`）即为 (甲) 位点。修复形状取本书**自己的**既有规范 —— ch560 L61 本来就有 `"你为什么不回答？"维护派执行者的声音在重叠。"锚点A0428碎片在哪里？"`，即 `"台词"叙述。"台词"`，两段台词各带各自的收尾标点；ch564 L181 `"轩哥？"赵大嘴的声音在0429的信号中。"你看到了什么？"` 同形。补完的 ch564 L79 已与父本 L85 **逐字相同**。
2. **`说：`/`继续说：` 一律不动。** V4 语料（未受本轮影响）同时存在裸文体 `余温说：那些记忆里面的感情…` 与引号体 `它说："小叶，你做了一个很重要的选择。"` —— 两种并存是既定写法，不是剥引号的伤。据此 ch565 L27、ch567 L101、ch569 L41 段、ch570 L129/L139 全部未动。
3. **`「」` 一律不动。** ch564 L115/L161 的 `「选择B路径，伪装为拖油瓶。维持闭环稳定性。」` 是 §一·补 9 认定的标签体（系统指示文本），L161 的裸文版是同一文本的转述，不是缺引号。
4. **不动的三类**：自由间接引语（ch564 L183 `告诉他你看到了什么`）、间接引语（ch565 L73 `但赵大嘴说，从第一次…`）、叙述与台词粘连的合成句（ch567 L43 —— 前半是叶文轩说话，后半 `它在叶文轩的系统终端和赵大嘴的手机之间架了一座桥。` 与同章 L15 的旁白逐字重复，需要改写而非加引号）。**这三类需作者定夺，不是本批遗漏。**

**本批暴露、已量化、按计划未动的三类新伤：**

- **半角撇号族**：ch566 拿 `'` 当引号且**不收尾**（`然后电话那头有人说：'你好，请问是叶文轩先生吗？`）。**全 V5 166 处 / 38 章**，本带只 ch566 的 11 处，其中 3 处台词位点已在 B1b 内改回 `"` 并补上收尾引号；其余 35 章 155 处待另立一轮。
- **强调引号残渣 `静`**：剥引号删了 `"静"` 的引号、把字留下了（`…一点波澜都没有。静，但赵大嘴感到下面的重量。`）。**全 V5 只 4 处，且全在本带**（ch561/569/570）。这是**字**的伤不是标点，§八未授权，未动。同族的 `他清了清喉*` 见 R249 待办。
- **模板化复读块**：ch570 L41/L89/L137 与 ch569 L81/L89 共用同一句旁白（`叶文轩的声音…0429备份的传输质量已经很好，叶文轩能正常说话，赵大嘴能正常听到。`），且句中缺句号（`传荡开来0429备份`）。本批只补台词引号，旁白未动。

**验收：** 1/2/3/4/5/6/8 过 —— CJK 逐章相等（20/20）、≥3000（20/20）、引号偶数、L1+DEADLY 不涨、112 对全为台词（人工逐行读过）、`--verify` 从 HEAD 重放 69 处复现工作区。**第 7 条未达**：本带密度 **6.3 → 9.3/千**（ch567 17.8 最接近）。与 B1a 结论一致 —— **纯补台词也到不了 18–28**；剩余缺口是「无引述结构的纯台词」，只能通读。

**工具（B1b 新增）：** `tools/r252_b1b_sites.json`（69 行清单）+ 底稿 `.claude/tmp/r252_b1b_plan.txt` 与 `.claude/tmp/r252_b1b_build.py`。构建器**不转录正文**，只给切点标记，切点逐字符从文件里取，并断言：切点在行内唯一、首段若为台词必须位于行首（否则会把叙述前缀卷进引号）、`subs` 规则在段内唯一命中。三条断言各拦下一次真实误编辑，全部在写盘前 ABORT。`tools/fix_r252_b1a.py` 增 `--sites`（默认值不变，B1a 调用式不受影响），两表共用同一台 fail-closed 引擎。

**B2a（2026-09-24，提交 `b57750e2`）** —— 标点半批，83 行 / 14 章（ch587 一章占 28 处），只做 §三 授权的第 2 类编辑。

1. **崩坏模型定型。** 强调引号被摊平后写作 `。"X"。`（句号被搬进了新引号对），随后剥离轮跑 `。"`→`，` 并删掉引号字符，于是句子边界从关键词**之后**跑到了**之前**，成了一个逗号。逆变换因此是均匀的：删逗号，并在父本有句号而现值没有时把句号补回关键词之后（乙1／乙2）。已用 B1a 的验证样本（`预置的，记忆提取模块。`→`预置的记忆提取模块。`）与父本形态（`，。"X"。` 同位语保留逗号）双向核对。
2. **本带认证类已清空（反证法）。** 把修好的正文重新喂回真正的规划器 → **提案 0 条**。做法是先把已提交的表备份、跑完再还原并核对 md5（`eef23d8562a2a5cf38d61f25ea849298`）—— 规划器尾部是可执行代码，`import` 它会重跑并覆盖表。
3. **丙类整行台词 4 处**补引号（ch574 L107/L141、ch575 L5、ch585 L68）：父本整行带引号的台词，剥离轮把引号全删了。
4. **三类未动（§八 未授权）。** ①**游移句号 7 处**：后来的某一轮把剥离轮的逗号改回了句号，但句号仍留在关键词**之前**；②**缺失句号 212 处**：父本有 `。` 而现值什么都没有，两句直接粘连（`光晕在慢慢变淡0428碎片的锚点还在`）；③**79/83 行在忽略引号后仍与父本不同，但这不能当"残留伤"引用** —— 其中大部分正是父本自身被制造出来的句号，规则本就要删。三项明细存 `.claude/tmp/r252_b2_residue.txt`。
5. **验收**：1/2/3/4/5/6/8 过（CJK 逐章相等 20/20、≥3000、引号偶数、L1+DEADLY 不涨、83 处 `--verify` 从 HEAD 重放复现工作区）；**第 7 条未达**：本带 9.2 → 9.3/千 —— 标点几乎不动密度，缺口全在台词引号，故紧随其后做 B2b。

**B2b（2026-09-24，提交 `94a8a2cb`）** —— (甲) 类补引号，96 行 / 18 章 / **新增 122 对引号**。检测器本带提出 131 个候选，35 个放掉（96 + 14 + 21 = 131）。

**本批判断规则（比 B1b 更保守，两条硬判据）：** 只有**与归属语相邻的那一段**是"经认证的台词"，加引号；归属语**之后**的句子，仅当文本自证是台词时才加 ——（a）含第一/第二人称代词（`我/我们/你/你们`；本书叙述通篇第三人称，叙述不会对叶文轩称"你"），或（b）是问句（`她是谁？`、`你感到吗？`）。其余一律留在引号外。

- 这条规则当场拦下一处：ch578 L49 的 `她的语气很平淡静，` 是叙述从句，构建器首版把它卷进了台词（`"我信任你父亲，"沈知秋说。"她的语气很平淡静，所以我相信你。…"`），已改成 `"我信任你父亲，"沈知秋说。她的语气很平淡静，"所以我相信你。…"`。**裸尾不是伤，错发的引号才是** —— 这是本批相对 B1b 的方法差异：B1b 对归属语之后的段多数加了引号（`(553,29)`、`(567,35)` 等），B2b 默认不加。差异方向保守，且 39 处裸尾已单列成清单（`.claude/tmp/r252_b2b_residue.txt` 第一节）待作者裁定；作者若认为该加，是在既有行上再插一对引号的一步操作。
- **判据②首次拿到父本证据。** ch571 L179、ch575 L137、ch580 L119 三行 `0428碎片在回廊的第四层中说：…`，其父本**同样写成裸冒号**（父 L313／L153／L123）。剥离轮之前的父本就这么写 —— 裸冒号体是本书自己的写法，不是剥离轮的伤。本带共 30 处 `说：` 行，一律未动，其中 14 个进过候选。
- **21 个候选根本不是台词**：纯叙述碰巧含言语动词（`他摸不着该从哪里开口`、`0428碎片的粉色光在脉动，在等叶文轩回应`），或言语动词落在别的词里（`陈小鱼就会开口`）。
- **本带新量化、未动的两类**：①**裸尾 39 处**（见上）；②**以名词短语作归属语的台词 14 处** —— `赵大嘴的嗓音干得发涩。`、`那里是什么？赵大嘴的声线有些发干。`、`他清楚我会来？叶文轩的声调发涩。` 这类，检测器要求"人名 + 言语动词"故提不出候选，其中 9 处显然是没引号的台词。§八 未授权，未动。
- **半角撇号**：本带仅 ch575 L35 一对（`声音说：'你的女儿还在。…'`），在已改行上顺手改回 `"`；其余 155 处仍待专轮。
- **验收**：1/2/3/4/5/6/8 过（CJK 逐章相等 20/20、≥3000、引号偶数、L1+DEADLY 不涨、96 处 `--verify` 从 HEAD 重放复现工作区）；**第 7 条未达**：本带 9.3 → 12.6/千。章间极不均：ch579 31.2、ch575 26.0、ch572 25.2 已达标，而 ch588 3.3、ch571 4.0、ch582/ch584 5.8 差得远 —— 剩下的正是 §八 说的**「无引述结构的纯台词」**，检测器数不到，只能通读。
- **工具**：`tools/r252_b2b_sites.json`（96 行清单）+ `.claude/tmp/r252_b2b_build.py`（B1b 构建器的副本，只改输出路径与 SPEC，两批的表互不覆盖）。

**B3a（2026-09-24，提交 `cc819216`）** —— 标点半批，159 行 / 19 章（ch596 本带无位点），只做 §三 授权的第 2 类编辑。

1. **崩坏模型连续第三带成立。** 用 B2a 的同一台规划器跑本带（`.claude/tmp/r252_b3_plan.py`，只改输出路径与默认范围）→ **159 条提案、全部以父本为证**，无一条需要新判据。分类：**乙1 121 行（单点）／乙2 31 行（两点）／乙3 3 行（三点）＝ 192 个逗号位点**；另 **丙 4 行整行台词**补引号（`"嗯？"`：ch592 L175、ch598 L173、ch603 L129、ch604 L69）。乙类不新增引号，故本批只 +4 对。
2. **一行按规则处理、留下词级缺陷**：ch609 L99 `等了一个，很多年。` → `等了一个很多年。` —— 逗号可证是剥离轮残渣（父 L107 写 `等了一个。"很多年"。`），但 `一个` 是**词**的伤，§八 未授权，已入待办。
3. **验收**：1/2/3/4/5/6/8 过（CJK 逐章相等 20/20、≥3000 20/20、引号偶数、L1+DEADLY 不涨、159 处 `--verify` 从 HEAD 重放复现工作区）；**第 7 条未达**：本带 5.3 → 5.4/千 —— 与 B1a/B2a 同结论，标点不动密度，缺口全在台词引号，故紧接着做 B3b。验收明细存 `.claude/tmp/_b3a_acc.txt`。
4. **工具**：`tools/r252_b3_sites.json`（159 行）+ 复用 `tools/fix_r252_b1a.py`（引擎未改）。审计 `LEGACY_IMPACT_RESULTS/20260924-33`（`transform`）／`-34`（`main`），gate=PASS、raw_matches=0。

**B3b（2026-09-24，提交 `df79d959`）** —— (甲) 类补引号，114 行 / 20 章 / **新增 203 对引号**（父本推导 58 + 人工裁决 56）。

1. **判据升级为「父本权威」。** B1b/B2b 靠行内自证（人名 + 言语动词）认台词，本批第一次把**父本 `9252b142^` 当作引号位置的唯一权威**：剥离轮吃掉的两类引号对中，「台词」是可证附伤（补回），「强调」（`"关键词"`）是有意为之（永不补回）。由此得四条硬规则：①强调对、半角撇号、`124年`/`121年` 这类**用词差异照现值**；②现值丢掉的标点**不补**（§八）；③父本无对应行（被改写过）的，只引**紧邻归属语之前**的那片台词；④父本整行即台词的按父本补。58 条来自「引号/撇号/逗号盲键」对齐的推导（对齐 70/126），其余 56 条与父本逐行并读后裁决 —— 剥离轮把引号周围的叙述（归属语、尾句）也改写过，位置搬运在那里不可信。
2. **检测器的一处形状缺陷被校正。** `is_dialogue` 的 `NAME = [一-鿿]{2,4}` 匹配不到五字人名 `未来叶文轩`，`未来叶文轩说，` **之前**的台词因此被系统性漏判；本批把该形状放宽到 `{2,5}`，推导数 42 → 58。
3. **本批保留、登记在案的**（明细 `.claude/tmp/r252_b3b_residue.txt`）：**12 处刻意未动**（纯强调行、父本同写裸冒号或行内无台词结构的位置）；**12 处「裸尾」**经与父本逐条对读，确认**父本同位置写的也是叙述**，不是遗漏（与 B2b 那 39 处性质不同）；**2 处缺逗号**（ch598 L107、ch605 L11）按 §八 不补，现值成 `"台词"X说。` —— 该形状全书仅 4 例（对照 `X说，"…"` 1,460 例），作者若判「补」，每行插一个 `，` 即可；**3 处句级受损**（ch597 L104 的尾句 `因为选C答。`、ch608 L141 的叙述/台词粘连、ch609 L13 的直接引语改写残留）须改写而非加引号。半角撇号按规则原样保留（本带 ch597 L60、ch601 L99）。
4. **验收**：1/2/3/4/5/6/8 过（CJK 20/20 逐章相等、≥3000 20/20、引号偶数、L1+DEADLY 不涨、114 处 `--verify` 从 HEAD 重放复现工作区）；**第 7 条未达**：本带 5.4 → 11.2/千。章间不均：ch597 +30、ch599 +19、ch608 +17、ch609 +14 最重，ch596 +1、ch593 +2 最轻 —— 剩余缺口仍是 §八 说的「无引述结构的纯台词」，检测器数不到，只能通读。
5. **工具**：`tools/r252_b3b_sites.json`（114 行）+ `.claude/tmp/r252_b3b_gen.py`（切点标记法：不转录正文，切点逐字符取自文件；断言切点行内唯一、首段若为台词必须居行首、`subs` 唯一命中、CJK 序列不变 —— 本批 dry run 四条断言均未触发 ABORT）。审计 `-35`（`build_line`）／`-36`（`load_derived`）／`-37`（`transform`）／`-38`（`main`），全 gate=PASS、raw_matches=0。
6. **由此提出的后续（待作者裁定）**：「父本权威」既然可行，B1b/B2b 的尾部欠补（父本对照下约 39 处）与 B2b 单列的 39 处裸尾，建议另立一轮**「父本权威回扫 ch551–750」**统一裁一遍，而不是零散补丁。

**B4a（2026-09-24，提交 `a6bef128`）** —— 标点半批，29 行 / 7 章（本带 20 章中 13 章无位点），只做 §三 授权的第 2 类编辑。

1. **崩坏模型连续第四带成立。** 复用 B3a 的同一台规划器（`.claude/tmp/r252_b4_plan.py`，只改输出路径与默认范围）→ **29 条提案，全部以父本 `9252b142^` 为证**，无一条需要新判据，无一条需人工改写。分类里 **丙 1 行整行台词**（ch618 L59：父本整行是一句带引号的台词，剥离轮把引号删光，现值成了裸句）。乙类不新增引号，故本批只 +1 对。
2. **本带可证位点本来就少。** 20 章里 13 章一条提案也没有 —— 不是规划器失灵，而是剥离轮在这一带**大段重写过叙述**，能证出 `。"`→`，` 逆变换的只剩这么几处。缺口全在台词引号，故紧随其后做 B4b。
3. **验收**：1/2/3/4/5/6/8 过（CJK 逐章相等 20/20、≥3000 20/20、引号偶数、L1+DEADLY 不涨、29 处 `--verify` 从 HEAD 重放复现工作区）；**第 7 条未达**：本带 6.0 → 6.1/千 —— 与 B1a/B2a/B3a 同结论，标点不动密度。
4. **工具**：`tools/r252_b4_sites.json`（29 行）+ 复用 `tools/fix_r252_b1a.py`（引擎未改）。审计 `-40`（`plan`）／`-41`（`apply_sites`）／`-42`（`transform`）／`-43`（`main`），全 gate=PASS、raw_matches=0。

**B4b（2026-09-24，提交 `f74253a8`）** —— (甲) 类补引号，69 行 / 17 章 / **新增 100 对引号**（父本推导 3 + 人工裁决 66）。底稿对全带 82 个候选做对齐，只有 3 个对上；落入手工裁决的 79 个里，**13 个放掉**（66 + 13 = 79）。

1. **本带的自动对齐率是全轮最低的，父本改作「人工定位后逐行并读」。** 79 个手工候选里，77 个被判「父本未对齐」，另 2 个另有原因（ch614 L99 行内已有引号、ch625 L37 父本那一对不是台词对）。于是把每个候选都放回父本人工找对应行：**27 处找到并据此定夺**（如 ch629 那串对话、ch630 L23 守门人那段、ch611 L178 的 `"我得走了，"`），其余候选项的父本已被整段重写，只能按行内形状判。四条判据：①父本有可比行的，按父本还原**台词**对、**永不还原强调对**（`"关键词"`）；②父本无可比行的，只引**紧邻归属语之前**的那片；③归属语**之后**的句子沿用 B2b 的保守判据 —— 归属语以 `，` 收尾（话还在继续）**且**该句读起来是台词才加，`声调平得没有起伏。`、`走廊很静。` 这类描写说话人的句子一律留在引号外（ch625 L53、ch629 L33）；④现值丢掉的标点不补（§八）。
2. **13 个放掉的分两类。** ①**6 处 `说：` 裸冒号体**（ch611 L117、ch612 L25、ch613 L119/L129/L131/L133）：判据②，本书自己的转述写法，不是剥离轮的伤；②**7 处不是台词**（ch618 L108、ch619 L79、ch620 L87/L89、ch628 L59、ch629 L13/L19）：纯叙述行碰巧含言语动词，或言语动词落在别的结构里。
3. **构建器本批的两处修订**（都在写盘前拦下真实问题）：①**锚点可向前加长** —— ch616 L45 与 L222 是**同一条句子出现两次**（`好看，叶文轩说。`，父本同样两处），整行锚点不唯一；改为逐行向前扩上下文直到唯一（该两处各扩 2 行），锚点仍是内容而非行号，`--verify` 与行尾校验照常通过。②**行首切点不再要求「行内唯一」** —— ch623 L29 的 `同意，` 在同一行出现两次（行首、`你在意识中说同意，`），但首段切点位于 0 位，左端匹配是唯一读法；现在只对**非行首**切点要求行内唯一。
4. **验收**：1/2/3/4/5/6/8 过（CJK 逐章相等 20/20，61183 → 61183；≥3000 20/20；引号偶数；L1+DEADLY 不涨；69 处 `--verify` 从 HEAD 重放复现工作区）。**第 7 条未达**：本带 6.1 → 9.3/千（B4a+B4b 合计 +100 对）。章间不均：ch629 +27、ch621 +15、ch624 +12、ch625 +10 最重，ch612/ch618/ch619 一章未改 —— 剩余缺口仍是 §八 说的「无引述结构的纯台词」，检测器数不到，只能通读。
5. **工具**：`tools/r252_b4b_sites.json`（69 行）+ `.claude/tmp/r252_b4b_gen.py`，决策表 `.claude/tmp/r252_b4b_manual_spec.txt`（一行一位点，格式 `章|行|父行|('切点','标签')…`，切点逐字符取自文件，不转录正文）。审计 `-46`（`load_derived`）／`-47`（`load_manual`）／`-48`（`build_line`）／`-49`（`make_anchor`）／`-50`（`main`）／`-51`（`transform`）／`-52`（`main`），全 gate=PASS、raw_matches=0。

**B5a（2026-09-24，提交 `f731e736`）** —— 标点半批，234 行 / 19 章（ch649 本带无位点），只做 §三 授权的第 2 类编辑。

1. **崩坏模型连续第五带成立，但规划器抓出一处规则缺陷。** 复用 B4a 的同一台规划器（`.claude/tmp/r252_b5_plan.py`，只改输出路径与默认范围）→ 提案 231 条、全部以父本 `9252b142^` 为证；分类 **乙1 212 / 乙2 5 / 乙3 2 / 乙4 2 ＝ 236 个逗号位点**，另 **丙 10 行整行台词**。**跑 dry 之前**逐条并读父本时发现两条提案会写出 `。，` —— 这是前四批标点半批合计 243 处补句号里从未出现过的形状。父本原件是 `也会。"失效"。，维护派`：剥离轮只在 X 收句时才把句号搬过 X，X 收在 `，` 上（`也会失效，维护派`）时它另写了一个句号、把原逗号留在后面。故护板改为读右引号**之后**的两个字符，遇 `。，` 一律不补句号。修完重跑，表 diff 恰好这两行变化，全表 `。，` 计数 0（ch631 L288、ch636 L329）。**"父本给的是原文想说什么，不是原文是什么"**（§一·补 7）在本带第一次由代码兑现。

2. **三行手工位点：行尾孤立引号，不是剥离轮的伤。** ch638 L257、ch643 L223、ch647 L150 各是一行叙述以孤立引号收尾（`赵大嘴的喉咙发不出利索的，"`）。剥引号轮是 `9252b142`（08-22），而该串首见于 `9cd5e651`（08-25「Voice rounds R133-R138」）——**晚于剥离轮**；同一轮把同一模板写成 `……`×2、半角 `,`×4、`。`×3，故 `，"` 在它自己那一轮里就是异形。父本 `9252b142^` 里**没有 `利索` 行**（这三行所在段落已被整段重写），无处可还原；且该引号既不开也不合，是行内唯一的引号。§五 第 1 条要求每章引号成对，最小修复＝删掉它、保留语音轮的用词。**这是本轮的第三类编辑，全卷仅此三处**；另 13 行以 `，"` 收尾的都带开引号，一律未动。完整清单存 `.claude/tmp/r252_tail_quote_family.txt`。

3. **新登记：孤行引号族（全 V5 42 行 / 5 章）。** 台词的收尾引号被挪到下一行独占一行（`"大嘴，"叶文轩说，"你看地面上的纹路。` ／ 下一行一个 `"`），分布 ch552 18、ch563 2、ch617 16、ch618 2、ch679 4 —— **本带 631–650 内 0 处**，五章都在已完成的批次（552/563→B1，617/618→B4）或尚未扫到的章（679）。修复＝把孤行引号并回上一行行尾，属另一轮，本批未动。

4. **验收**：1/2/3/4/5/6/8 过（CJK 逐章相等 20/20，78292 → 78292；≥3000 20/20；每章引号偶数，且**全带没有一行引号数为奇数**，即跨行对 0；L1+DEADLY 不涨；234 处 `--verify` 从 HEAD 重放复现工作区）。**第 7 条未达**：本带 9.3 → 9.6/千（+7 对＝丙 10 对 − 手工 3 对）。章间不均：ch631 35 行、ch635 35 行最重，ch641/ch650 各 1 行。

5. **工具**：`tools/r252_b5_sites.json`（234 行）+ `.claude/tmp/r252_b5_plan.py`。本批对规划器做了两处修订：①**手工行可以在无对齐父本的行上触发** —— 孤立引号那三行落在父本整段重写过的区域（`pi is None`），原逻辑会跳过它们、再因「HAND 行未触发」整体 ABORT；②清单里无对齐行显示为 `（无对齐）`。审计 `-58`（`plan`）／`-59`（`transform`）／`-60`（`main`），全 gate=PASS、raw_matches=0。

**B5b（2026-09-24，提交 `1b58e739`）** —— (甲) 类补引号，22 行 / 8 章 / **新增 35 对引号**（父本推导 6 + 人工裁决 16）。底稿对本带 176 个候选逐条处置：16 个落实，160 个放掉。

1. **本带的形状：绝大多数候选是「归属语 + 嗓音描写」，台词在下一行。** 176 个候选里 24 个带父本台词证据、152 个没有；逐行读下来，放掉的几乎全是同一种行（`赵大嘴的鼻子在酸，赵大嘴开口，嗓音粗糙，干涩得厉害。`）—— 它的台词在**下一行**、原本就独立成段，剥离轮没碰过它。检测器（`is_speech`：`ATTR2_AFTER`/`ATTR2_BEFORE` + `VERB2` 收 `开口/回应/补充`）把嗓音描写误当成台词触发器，这是本批候选虚高的来源。

2. **15 个站点是「首对」，判据沿用 B4b 的 ch630 L99。** 父本把这些行写成两对（`"台词，"X说，"台词"`），但 `R.is_dialogue` 会把首段判成**非台词** —— 它收在 `，` 上，而归属语正则被剥离轮写在 `在"说"，` 里的内层引号挡断。这是**机械误判，不是读出来的结论**，父本即证据；先例已由 B4b ch630 L99 立下，本批照此办理。第 16 个是 ch642 L247 的整行包引号。

3. **3 处按规则未动。** ch634 L110：父 L124 是 `"他们'理解'了真相？"赵大嘴问。`，现值只剩 `赵大嘴问。` —— 台词文本被后面某一轮**词级**删掉，不在本轮两项授权内（§八 不许回填词）。ch636 L141、ch637 L41：`"母亲"在"信"里"说"` 这类**逐词强调引号**是父本自身被制造出来的伤，B3b 规则①与 §八 明令永不还原。

4. **新登记两族（均未动）。** ①**行首孤逗号族**：全 V5 14 行 / 6 章（`，副作用。`、`，但0429碎片不会，熄灭。`），其中 ch638/642/648 在本带内 —— R250 只清了 `。` 的同胞（`^。([一-鿿]{1,4})` → `\1`，1,185 处），`，` 变体无人处理。②**乙类规划器的「整行对齐」盲区**：父本 `从。"天上"。` → 现值 `从，天上。`（ch632 L226）这类**行首被重写过**的 `。"`→`，` 残渣，因整行对不上而提不出提案，属 §九 已提的「父本权威回扫」范围。

5. **验收**：1/2/3/4/5/6/8 过（CJK 逐章相等 20/20，78292 → 78292；≥3000 20/20；每章引号偶数且无 `""`；L1+DEADLY 不涨，4 章有命中且前后不变；22 处 `--verify` 从 HEAD 重放复现工作区）。**第 7 条未达**：本带 9.6 → 10.4/千（+35 对）。章间不均：ch632 +9、ch637/ch638 各 +7 最重，12 章无位点。

6. **工具**：`tools/r252_b5b_sites.json`（22 行）+ `.claude/tmp/r252_b5b_gen.py`（B4b 构建器的副本，只改输出路径、SPEC 与 `kind`），决策表 `.claude/tmp/r252_b5b_manual_spec.txt`（`章|行|父行|('切点','标签')…`）。审计 `-62`（`load_derived`）／`-63`（`load_manual`）／`-64`（`build_line`）／`-65`（`make_anchor`）／`-66`（`main`）／`-67`（`transform`）／`-68`（`main`），全 gate=PASS、raw_matches=0。

**B6a（2026-09-24，提交 `1c0a6974`）** —— 标点半批，91 行 / 11 章（本带 20 章中 9 章无位点），只做 §三 授权的第 2 类编辑。批号走 §四 分批表（工具与表名仍为 `r252_b9_*`）。

1. **崩坏模型连续第六带成立，但规划器撞上两种它看不见的形状。** 复用 B5a 的同一台规划器（`.claude/tmp/r252_b9_plan.py`，由 `.claude/tmp/r252_b9_make_planner.py` 按替换规则从 B5 派生，派生脚本对每处替换断言命中次数）→ 提案 91 条、全部以父本 `9252b142^` 为证。分类：**乙1 60 / 乙2 19 / 乙3 3 / 乙5 2（新形状：一行五个位点）／丙 3 行整行台词／手 4 行**，合计 117 个逗号位点。乙类不新增引号，故本批只 +3 对。

2. **4 条手工行 = 两种新形状。** 本带唯一需要读句子的地方；其余 87 行由规则直接给出，并用「把手工表关掉重跑一遍、逐行比 before/after」的办法核过 —— 除这 4 行外一字不差（`.claude/tmp/r252_b9_handdiff.py`）。**这两行也是本轮的第四类编辑（原地改标点，不动词）。**

   - **父本的 `。"` 是「句子句号 + 开引号」，不是强调包。** ch728 L17（`"感到维护派的人在撤退。"赵大嘴突然说。…`）、ch729 L51（`叶文轩愣了。"终止"，0224用的是准确的词`）的右引号后紧跟**归属语或逗号**，规划器于是把 X 读成「到下一个引号为止」的整段 —— ch728 L17 读进了 21 个字的后文，删掉逗号会把台词粘到自己的归属语上（`撤退赵大嘴突然说。`）。剥离轮在这里吃掉的是**句号**，故修复是**原地把 `，` 改回 `。`**（§三 第 2 类），两个字符全部以父本为证；按 §八 **不插逗号**，ch729 L51 的 `终止0224` 仍是词级粘连。ch728 L17 的台词引号属 (甲) 类，留给 **B6b**。
   - **句子边界从 X 之后跑到了 X 之前。** ch662 L207 与 ch663 L97 是**同一句话出现两次**：父本 `朵朵的"眼睛"在"看。"空气"。"朵朵"的"嘴"在。"动"。`，现值 `朵朵的眼睛在看，空气朵朵的嘴在动。`。规则的补句号只认右引号**之后**的句号，而这里句号在左引号**之前**，提案于是成了「删逗号、什么都不补」→ `在看空气朵朵的嘴在动。`。读到句子才能定：眼睛在看空气，嘴在动。同章 L193／ch663 L81 的 `…抬了，起来朵朵的眼睛在看空气，在看，叶文轩。` → `…抬了起来。朵朵的眼睛在看空气，在看叶文轩。` 是规则自己给出的 `。 …朵朵` 同形先例。

3. **15 处「删逗号、不补句号」的粘接位点，登记未改。** 全带 19 处「删逗号且两侧都是汉字、却不补句号」的位点里，4 处即上条手工行；其余 15 处的父本句号是**句号链噪声**（父本一行连包 5–6 个词，每个右引号后都跟着一个被制造出来的句号，如 ch654 L29 的 `在光体中。"交织"。"蓝色"是0429的。"纹路"。`），或 `。，` 护板认定的「原标点就是逗号」（ch654 L153/L195）。§八 明令**不动**这类不可证的 `。`/`，` 差异，故只删逗号、不补句号。明细：ch653 L67/L151、ch654 L29（2 处）/L31、ch659 L182、ch661 L79/L107/L174/L192、ch662 L63/L77/L150。**该族在已交付批次里同样存在**（B1 3 处、B4 1 处、B5 4 处），本带偏多是因为 ch654/661/662 那一段的强调引号灾难更重。

4. **本带的位点集中在同几句模板句上。** `他能看见一个花园，小区的，花园有，花坛有，长椅有，滑梯有，一棵大榕树。`（ch661 L73、ch662 L57，各 5 个位点 = 本带两条乙5）、`朵朵的眼睛在看，空气朵朵的嘴在动。`（ch662 L207、ch663 L97）、`朵朵的头抬了，起来朵朵的眼睛在看空气，在看，叶文轩。`（ch662 L193、ch663 L81）、`叶文轩能看见了，他又能看见，现实…`（ch661 L192、ch662 L150）—— ch661/662/663 三章是同一段场景的复写。这既印证了 §一·补 的模板化复读判断，也说明带内读句子的成本主要花在这几句上。

5. **验收**：1/2/3/4/5/6/8 过（CJK 逐章相等 20/20，68709 → 68709；≥3000 20/20；每章引号偶数且无 `""`；L1+DEADLY 不涨；91 处 `--verify` 从 HEAD 重放复现工作区）。**第 7 条未达**：本带 6.2 → 6.3/千（+3 对，全是丙类整行台词）—— 与 B1a–B5a 同结论：标点半批不动密度，缺口全在台词引号。章间不均：ch654 20 行、ch652 19 行、ch653 13 行最重；655–658、660、664、725、726、730 九章无位点。验收明细存 `.claude/tmp/_b9a_acc.txt`。

6. **工具**：`tools/r252_b9_sites.json`（91 行）+ `.claude/tmp/r252_b9_plan.py`（本批对规划器的唯一修订是**手工表**：`HAND` 由空表改为 4 行，逐条写明判读理由；「HAND 行未触发即 ABORT」的检查沿用）。审计 `-70`（`plan`）／`-71`（`repair_sites`）／`-72`（`apply_sites`）／`-73`（`transform`）／`-74`（`main`），手工表写定后补 `-76`（`plan`）；本记录的审计 `-77`（`R252`）。全 gate=PASS、raw_matches=0。

**B6b（2026-09-24，提交 `bbaf83c6`）** —— (甲) 类补引号，43 行 / 10 章 / **新增 76 对引号**（父本推导 16 + 人工裁决 27）。底稿对本带 96 个候选逐条处置：27 个落实，69 个放掉。

1. **放掉的大头是裸冒号体，集中在 ch656 一章。** 69 个放掉的候选里 47 个是 `X说：…`／冒号收尾的整句（ch656 独占 23、ch663 18，余在 654/655/659/661），另 5 个是同体的续句或行内引导句（ch654 L222、ch655 L77、ch659 L194 是 `说：` 之后另起一行的续句；ch656 L193/L373 是 `继续说，声音…：<台词>` 的行内冒号）。父本对这 52 处同样写裸冒号（`0428备份在"说"：但"执行"选项C之前，有一个。"问题"。`），剥离轮没碰过它们 —— 判据② 首次以整章规模出现，一律未动。

2. **16 个是检测器误报，父本可证不是台词。** `叶文轩等赵大嘴说。`、`他想要回答，但不知道该说什么。`、`光体在脉动，在等待。光体在等叶文轩说。`、`他想开口，发不出声音。`（ch664 L15）这类行里 `说`／`开口` 落在别的结构上；ch726 L7、ch729 L43 两行的父本与现值**逐字相同且父本无任何引号**，直接排除 —— 这两行也是「父/原均无记录」的候选里唯一的两个。

3. **自动对齐率是全轮最低的一档：27 个候选里 22 个靠人工回父本接上。** 剥离轮把 ch727/728 的归属语与尾句一并改写过（`赵大嘴说。` → `赵大嘴开口。`、`声音有些哑` → `嗓音低低沉沉`），引号／撇号／逗号盲键因此对不上。用 B4b 的老办法（`.claude/tmp/r252_b9b_probe.py`：拿行内特征串回父本 `9252b142^` 检索）把 22 个候选重新接上父本，其中 **ch727 L41/L91/L95、ch728 L89/L91 五行的父本尾句本来就写在引号里** —— 这五行于是从「父本无对应行」升格为规则①，头尾两对一起补回。另 16 处由盲键自动对齐并逐对自检通过（ch727 4 行、ch728 12 行），其中 **ch728 L17 正是 B6a 第 2 条留到这里的那一处**，本轮由父本权威机器自己补上。剩 5 个候选（ch657 L25/L61/L89/L93、ch728 L196）确无对应行，按行内形状判。

4. **1 处裸尾登记未动。** ch657 L21（`0428备份说，这是共识的第一层，一个意识确定了自己的方向。…`）的台词在归属语**之后**、不含第一/第二人称也不是问句，按 B2b 判据留在引号外；同章 ch657 L93 只补了归属语之前的 `"记忆需要一个桥梁，"`，尾句同样留在外面。**裸尾不是伤**的口径继续有效。

5. **2 处机械误判，父本即证据。** ch728 L162/L212 的 `0224。`（叶文轩对着空气叫名字）在父本里是引号对，但内容不含 CJK，`is_dialogue` 第一句就返回 False。B5b 的 15 处「首对」同例，补回并登记：本带 76 对新增引号里仅此 2 对未过 `is_dialogue`，其余 74 对全部通过。

6. **验收**：1/2/3/4/5/6/8 过（CJK 逐章相等 20/20，68709 → 68709；≥3000 20/20；每章引号偶数且无 `""`；L1+DEADLY 不涨；43 处 `--verify` 从 HEAD 重放复现工作区）。**第 7 条未达**：本带 6.3 → 8.5/千（+76 对）。章间不均极端：**ch728 +38 对（25.5/千）独占一半**，ch727 +15、ch657 +7；652、656、658–664、729 十章无位点 —— 位点全长在 ch727/728 那场「0224 学习与改名」的连续对话上。验收明细存 `.claude/tmp/_b9b_acc.txt`。

7. **工具**：`tools/r252_b9b_sites.json`（43 行）+ `.claude/tmp/r252_b9b_gen.py`（B5b 构建器的副本，由 `.claude/tmp/r252_b9b_make.py` 按替换规则派生；派生脚本对每处替换断言命中次数、并断言旧批号不残留）。审计 `-78`（`load_derived`）／`-79`（`load_manual`）／`-80`（`build_line`）／`-81`（`make_anchor`）／`-82`（`main`）／`-83`（`transform`）／`-84`（`main`），本记录 `-85`（`R252`）。全 gate=PASS、raw_matches=0。

**B7a（2026-09-24，提交 `dd4c78fd`）** —— 标点半批，36 行 / 10 章（本带 20 章中 10 章无位点），只做 §三 授权的第 2 类编辑。批号走 §四 分批表（工具与表名仍为 `r252_b10_*`）。

1. **崩坏模型连续第七带成立；本带第一次出现「规划器看不见」的位点，用整章盲键扫补回 2 处。** 复用 B6a 的同一台规划器（`.claude/tmp/r252_b10_plan.py`，由 `.claude/tmp/r252_b10_make_planner.py` 按替换规则从 B9 派生，派生脚本对每处替换断言命中次数）→ 提案 34 条、全部以父本 `9252b142^` 为证，无一条需要新判据。分类：**乙1 24 行（单点）／乙2 2 行（两点）＝ 28 个逗号位点／丙 8 行整行台词／手 2 行**。乙类不新增引号，故本批只 +8 对。

2. **2 条手工行 ＝ 整行对齐的盲区，不是规则算错。** 剥离轮把父本的那一行并进了邻行，父本整行对不上、`repair_sites` 因而从不运行。ch737 L57（父 `…一种他无法定义的。"完整性感"。沈知秋不是在朵朵旁边…`）、ch740 L9（父 `…是来自空间本身的。"边界"。闭环的边界。…`）。发现办法：把**现值整行**按「引号/逗号/句号盲键」回父本**整章文本**里定位（`.claude/tmp/r252_b10_gapscan.py`，唯一命中才报），全带恰好扫出这两处，外加 1 处**跨行位点** ch742 L206 —— 其父本邻域是有意的 `不是。"逃避"。` 删除，不是标点位点，未动。两条手工行做的仍是**乙类同一变换**（删逗号 + 补句号），只是依据换成手写的 `HAND_PARENT` 父本原文；把手工表清空重跑一遍、逐行比 before/after 核对：多出的恰是这 2 行，且无一条规则行被吞掉（`.claude/tmp/r252_b10_handdiff.py` 输出 `规则行 34 + 手行 2 = 36 条`）。

3. **8 处（丙）整行台词全部卡在 `is_dialogue` 的机械门槛上，父本与上下文即证据。** 八行都是独占一行的应答：ch742 L187 `"89.7%。"`（上一行 `"多少？"赵大嘴问。`）、ch742 L211 与 ch748 L81 的 `"嗯。"`（上一行是同一模板的长问句）、ch745 L13 `"嗯。"`（上一行 `"这是第47号留下的？"赵大嘴问。`）、ch744 L305 与 ch745 L307 的 `"对。"`（上一行同为 `"所以我得等你。"`）、ch746 L146 `"对。"`（上一行 `"所以都是半盲状态。"`）、ch748 L177 `"对。"`（上一行 `赵大嘴消化了几秒。"要走完三层才能去第四层？"`）。`is_dialogue` 要求内容含 CJK、短行对至少两个 CJK：`89.7%。` 一个汉字也没有，`嗯。`／`对。` 只有一字，故一律判 False —— 与 B6b 的 `0224。` 是同一类机械下限，父本即引号对，遂补回并登记。本带 8 对新增引号全在此列。

4. **新族已量化、按计划未动：碎片编号前的标点被剥离轮吃掉，全 V5 约 1.5 万处。** 父本对现值（ch551–750，即全卷）逐位点数：`。`+数字 12,986 → 1,488（差 11,498）、`，`+数字 5,034 → 1,629（差 3,405）。样本 ch740 L95 `…记忆数据和桥梁系统0429说…`（父 `系统。0429说`）、ch551 L41 `他的胸口0429碎片的位置`（父 `胸口，0429碎片`）——现值是一串粘连，父本可证句读，`git log -S` 把这一族钉在 `9252b142` 上。**未动的理由**：这是**第二代形状**（不是 `。"X"。` 的逆变换）、不在 §三 两项授权内，且已交付的六个带一律未动。建议另立一轮，交作者拍板。

5. **验收**：1/2/3/4/5/6/8 过（CJK 逐章相等 20/20，86553 → 86553；≥3000 20/20；每章引号偶数且无 `""`；L1+DEADLY 不涨；36 处 `--verify` 从 HEAD 重放复现工作区）。本批另加一道自有的检查：**表内每一行编辑前后的 CJK 序列必须相同**（只许动标点，一个词也不许碰），36 行全过。**第 7 条未达**：本带 25.4 → 25.6/千（+8 对，全是丙类整行台词）。本带 20 章密度普遍偏高（ch749 39.8、ch747 39.2、ch739 39.1/千），标点半批照样不动密度。章间不均：ch743 6 行、ch737/744/745/750 各 5 行最重，ch746 仅 1 行；731–736、738、739、747、749 十章无位点。验收明细存 `.claude/tmp/_b10a_acc.txt`。

6. **工具**：`tools/r252_b10_sites.json`（36 行）+ `.claude/tmp/r252_b10_plan.py`（本批对规划器的唯一修订是手工表：`HAND` 由空表改为 2 行，并新增 `HAND_PARENT`，供无对齐手工行在清单里显示父本依据；「HAND 行未触发即 ABORT」沿用）+ `.claude/tmp/r252_b10_gapscan.py`（章级盲键扫描）。审计 `-86`（`HAND_NEW`）／`-87`（`plan`）／`-88`（`repair_sites`）／`-89`（`apply_sites`）／`-90`（`whole_line_speech`）／`-91`（`widen`），本记录 `-92`（`R252`）。全 gate=PASS、raw_matches=0。

**B7b（2026-09-25，提交 `0e15c9ec`）** —— (甲) 类补引号，194 行 / 19 章 / **新增 287 对引号**（ch749 本带无位点）。底稿对本带 **188 个定案站点**逐条处置（前一轮已定的 68 处 + 本轮并入的 120 处）。批号走 §四 分批表（工具与表名仍为 `r252_b10b_*`）。**这是 551–750 自然序扫描的最后一批**；§四 表里的 B1–B3（665–724 坏父本带）仍在队列中。

1. **底稿整族看不见一种归属语：`X的声音／嗓音／声线／声调 + 形容词，`。** 台词长在这种归属语两侧（`0429。赵大嘴的声音平稳，那根锁链，我需要同意什么？`），而 ① 底稿的 `ATTR` 模式不覆盖这一形态；② R211 的反重复改写把归属语本身换掉了（`声音平稳` → `不再晃动`／`定住了`／`声线平直`／`嗓音平如直线静`），现值行与父本行**连一个可对齐的键都不共享**，逐行对齐的底稿因此整批漏掉。发现办法是反过来扫：取父本每一个成对引号的内容，回**现值整章文本**里找它是否裸露（`.claude/tmp/r252_b10b_gapscan2.py`，180 处表头 = **142 行**不同位点），再用新定位器 `.claude/tmp/r252_b10b_gen2.py` 为每一行找**作证的父本行**（要求该父本行的每一对可还原引号都在现值行里对位），得 **118 条**候选，逐条对着父本读完（复核稿 `.claude/tmp/r252_b10b_plan2.txt`，`−／+` 逐行）。

2. **本批定下的四条机器判据。** ①**父本行须完整对位**：一行父本若有一对可还原的引号对在现值里**完全找不到**，它就不能为这一行作证（该对是否属「有意删除」由 ② 处置）；②**缺席的对一律跳过，不因此作废整行**（§八 不回填词）；③**两侧护板**：对位段必须在父本里被自己的邻字或标点／行首行尾夹住（否则两字对 `够。` 会匹配进 `够不够` 里 —— ch731 L128 的误配就是这么发现并排掉的）；④**尾标点吸收**：父本包在引号里的收句标点一并纳入对位段，而剥离轮自己的 `。"` → `，` 替换**沿用现值**，不还原成句号（§八）。另有两条读出来的口径：**片段规则**（父本引号对里残留的片段，只要落在该对自身的边界上、且收在标点或行尾，就照台词引起来 —— `回来了。` 出自 `"好像回来了。"`）；**冒号体判据**（父本原地没有冒号的 `X：…` 是 R211 自己的重构，按判据 ② 不动；父本本身写成「冒号 + 引号」的照还原，ch737 L189 的 `0224，你能检测到吗？` 即此例）。

3. **118 条推导里：3 条放掉、5 条改写、5 条手工补。** 放掉 ch742 L244／ch744 L43（`回廊，是过渡，空间在…` 的卡顿体重写，定位器把叙述误配成父本引号对）、ch739 L209（`控制面板上的数字跳动：记忆导出100%。`，父本原地无冒号）。改写 ch733 L161（冒号体接在首对之后，只引首对）；片段规则兑现四处 —— ch731 L81（`"回来了。"`）、ch733 L45／L157（`"停不下来。"`／`"真的停不下来了。"`）、ch737 L183（`"沈知秋真的在里面？"`）；另把 R211 改写过、定位器对不上的**五处手工行**读出来补上（ch735 L69/L212/L218、ch737 L107、ch738 L109，其中 ch735 L69 的整句归属语 `0224的声音简短` 已被删光，只剩裸报告）。手工行的判读理由逐条写在并入器 `.claude/tmp/r252_b10b_merge.py` 与决策表 `r252_b10b_manual_spec.txt` 的抬头。

4. **定位器自身的两处缺陷，是逐行读的时候暴露的，改完重跑。** ① 对位段原来收在引号对的**最后一个汉字**上，父本包在引号里的收句标点于是掉成一个独立切片，永远锚不唯一（`切点不唯一 '。'`）—— 改为把尾标点吸收进对位段；② 两字对 `够。` 匹配进 `够不够`，给 ch731 L128 配错了父本行 —— 加两侧护板后该父本行同时被正确地取消资格。两条修订之后重跑：可成行 118／需人工 24，24 条逐条读过，5 条成行（上条），其余按上面的规则放掉并登记。

5. **39 对未过 `is_dialogue`：全是机械下限，已逐对回父本核过。** 其中 **26 对内容一个汉字也没有**（`"0429。"`、`"0224。"`、`"0428。"`、`"0224，"`），**12 对收在 `，` 上**、直接接归属语（`"你来了，"`、`"爸爸，"`、`"0224说，"`、`"回廊的第三层是我的办公室，"` …），**1 对是单字应答 `"嗯。"`**（`嗯。` 只有一字，够不到 `is_dialogue` 的两字下限）。逐对回父本 `9252b142^` 检索：**每一对都能在同一位置找到父本的引号对**，差别只在 R211 改写过的归属语（`声音很平静` → `嗓音平如直线静`、`声音很轻` → `声线轻得就像烟雾`、`声音变了` → `声音发生了变化`）。与 B6b 的 `0224。`、B5b 的 15 处「首对」同族。

6. **验收**：1/2/3/4/5/6/8 过（CJK 逐章相等 20/20，86553 → 86553；≥3000 20/20；每章引号偶数且无 `""`；L1+DEADLY 不涨（ch731/735/741/742 四章有命中且前后不变）；194 处 `--verify` 从 HEAD 重放复现工作区；表内 287 对逐对核过 `is_dialogue`，39 对机械下限见上条）。**第 7 条未达**：本带 25.6 → 32.2/千（+287 对）—— 与 (甲) 类前九批同结论；本带 20 章密度本就普遍偏高（ch733 50.2、ch739 45.8、ch747 43.1、ch731 41.1/千）。章间不均：ch735 25 行、ch731 19 行、ch734 16 行、ch733 15 行最重，ch742／ch746／ch732 各 5 行、ch747 3 行最轻，ch749 无位点。验收明细存 `.claude/tmp/_b10b_acc.txt`。

7. **登记（未动）**：① 剥离吃掉的 `。`（碎片编号前的标点族，见 B7a 第 4 条）本带仍留 ch744 L9/L53、ch746 L74/L130、ch747 L128、ch749 L9；② R211 的归属语残骸就长在本带这几行上（`赵大嘴的嗓音平如直线稳`、`的说话一成不变常`、`叶文轩的嗓音实而平稳得不带一丝晃动`、`话音轻得一片羽毛`、`赵大嘴。的胸口`／`叶文轩将视线转向赵大嘴。的胸口`）；③ 父本自身制造的伤（ch743 L101/L103 的 `。，`、ch735 父本的 `十五钟`）；④ 按判据 ②③ 放掉的 `传来：` 冒号体（ch749 L39）与 `「」` 标签体（ch737 L129、ch750 L349）；⑤ 强调对（ch746 L124 的 `系统之外`）；⑥ 无引号对可对位的叙述行（ch732 L47、ch736 L71、ch742 L282、ch744 L155、ch745 L157、ch746 L271）。

8. **工具**：`tools/r252_b10b_sites.json`（194 行）+ 决策表 `.claude/tmp/r252_b10b_manual_spec.txt`（188 站点）+ 新定位器 `.claude/tmp/r252_b10b_gen2.py` + 指纹扫描 `.claude/tmp/r252_b10b_gapscan2.py` + 并入器 `.claude/tmp/r252_b10b_merge.py`（重复站点在写盘**前**断言，幂等可重跑）+ 父子检索 `.claude/tmp/r252_b10b_find.py`。审计 `-01`（`r252_b10b_gen2.main`）／`-02`（`locate`）／`-03`（`r252_b10b_merge.main`）／`-04`（`r252_b10b_find.main`）／`-05`（`fix_r252_b1a.transform`），本记录 `-06`（`R252`）。全 gate=PASS、raw_matches=0。

**BP1a（2026-09-25，提交 `abcbb678`）** —— 标点半批，78 行 / 13 章（本带 20 章中 7 章无位点），只做 §三 授权的第 2 类编辑。批号走 §四 分批表，工具与表名仍为 `r252_bp1_*`：`bp1` = **坏父本第 1 带**，因 `r252_b1_*` 已被 551–570 那一带占用。**这是 665–724 坏父本带的第一批。**

1. **崩坏模型连续第八带成立，本带第一次要放宽规则才看得见位点。** 复用 B7a 的同一台规划器（`.claude/tmp/r252_bp1_plan.py`，由 B10 派生）→ 提案 **50 条**，全部以父本 `9252b142^` 为证：**乙1 10 / 乙2 8 / 乙3 4 / 乙4 5 / 乙5 4 / 乙6 8 / 乙7 4 / 乙8 3 / 乙9 3 ＝ 45 行 / 50 个逗号位点**，另 **丙 1 行整行台词**（ch675 L21：父本即 `"爸……"`，剥离轮把引号删光）。规则修订一处：坏父本带的剥离输入形状是 **`字"。"字`** —— 强调引号收在句号**之前**，句号不是缺口首字，位点于是整批隐形，可现值仍带着残渣逗号。缺口模式由 `^(，?)。"` 放宽为 `^(，?)"?。"`；价格当场量过（`.claude/tmp/r252_bp1_relax.py` 逐行比对旧/新答案）：本带 **+9 行、0 处回归**，9 行逐行读过（`三个维护派的人同时，失去了平衡。` → `…同时失去了平衡。` 等）。放宽不改变 `open_at` 的落点，其余代码一字未动。

2. **28 行落在「整行对齐」的盲区，用章级扫描读出来，写进规划器的 `HAND`／`HAND_PARENT`。** 本带的剥离轮把父本的行**并进邻行、拆散**过，盲键 token 数因此对不上，`repair_sites` 永不运行 —— 但价值点仍在。做法沿用 B7a 的两段式：① 按「引号/逗号/句号盲键」把**现值整行**回父本**整章文本**里定位，唯一命中才用（整行重放：**0 行**）；② 落在父本行内部的，在**章级**一次算出全部位点（片段：**28 行**）。`.claude/tmp/r252_bp1_gapres.py` 打印每行的 父／现／提 与逐站点 `X/keep/add`（复核稿已读全）。28 行**不是手打清单**：`.claude/tmp/r252_bp1_handgen.py` 用同一套函数把结果写进 `HAND`／`HAND_PARENT`（marker 之间，28 行 / 28 条父本依据），一行一注；`plan` 照旧做「anchor 唯一命中」与「HAND 行未触发即 ABORT」两道检查，`handgen` 另断言每行 CJK 序列不变、且不得写出 `。，`／`，。`／`。。`。

3. **一处由读句子改判：ch672 L13。** 规则在 `朵朵` 后补句号（父本 `叶文轩能"看见。"朵朵"。朵朵"在`），但现值那一处还留着一个逗号，合成会写出 `朵朵。，朵朵` —— 前七批标点半批 243 处补句号里从未出现过的形状。父本在那一处的标点是 `"。`（句号在右引号**之后**），不是 `。"`，所以那个逗号不是这一对的残渣：留句号、删逗号。

4. **26 行「已带引号」的行单独查过，一处不欠。** 章级解析器对带引号的行一律跳过（(甲) 类留给下一半），但残渣规则读的是**盲键**（引号、逗号、句号都在键外），带引号的行同样可查。`.claude/tmp/r252_bp1_qcheck.py` 对全部 26 行跑同两段式，输出 **`引号行 26 条，其中可修 0 条`** —— 其中 ch681/ch684 两章是坏父本带里**已被后续轮次整章改写**、引号齐备的两章。

5. **登记（未动）。** ① **游移句号**：ch683 L11 `一道。看不见的波` —— 后来的某一轮把剥离轮的逗号改回了句号，句号仍留在关键词**之前**（同 B2a 第 4 条①）。② **缺失句号／模板复读块**：本带的粘连重于前几带（ch683 L57 `…在消退0429碎片在变远0429碎片…`、ch678 L31 `很暖朵朵的`、ch667 L89 `在踩实他的手在感知`），其父本句号是**相邻强调引号链上的噪声**（与 B5a 的 `。，` 护板、B6a 第 3 条的「句号链噪声」同族），按 §八 只删残渣逗号、不补句号。③ 章级扫描的分母：**无逗号 3,517 行 / 在表内 50 / 无法定位 not-found 1,451、ambiguous 14 / 无位点 27**（26 行带引号 + 1 行无站点）—— 与 551–750 前几带同量级。

6. **验收**：1/2/3/4/6/8 过（CJK 逐章相等 20/20，61781 → 61781；引号 516 → 518，每章偶数且无 `""`；L1+DEADLY 逐章不涨；78 处 `--verify` 从 HEAD 重放复现工作区；表内每行编辑前后的 CJK 序列相同，78 行全过）。**第 5 条未达**：ch675 = **2998 CJK**（本批只动标点，前后不变；既存待补，与 V6-820=2962 同列）。**第 7 条未达**：本带 **4.18 → 4.19/千**（+1 对）—— 与标点半批前七批同结论：标点不动密度，缺口全在台词引号。**第 9 条按作者指令跳过**（作者明确「不用读你直接继续改」）。位点分布：ch683 19 行、ch682 11 行、ch680 10 行最重；ch665/666/668/669/673/681/684 七章无位点。新增的唯一一对 `"爸……"` 未过 `is_dialogue`（内容只一个汉字，够不到「至少两个 CJK」的机械下限 —— 与 B6b 的 `0224。`、B7a 的 `嗯。`／`对。` 同族），父本即引号对，已登记。

7. **工具**：`tools/r252_bp1_sites.json`（78 行 = 50 规则行 + 28 手行）+ `.claude/tmp/r252_bp1_plan.py`（规则放宽一处 + `HAND`／`HAND_PARENT` 28 行）+ `.claude/tmp/r252_bp1_relax.py`（放宽价格）+ `.claude/tmp/r252_bp1_gapres.py`（章级解析）+ `.claude/tmp/r252_bp1_handgen.py`（手行写入）+ `.claude/tmp/r252_bp1_handprobe.py`（带引号行并读）+ `.claude/tmp/r252_bp1_qcheck.py`（引号行复查）+ 复用 `tools/fix_r252_b1a.py`（引擎未改）。审计 `-14`（`plan`／`repair_sites`／`covering`）／`-18`（`r252_bp1_handgen.main`）／`-19`（`r252_bp1_handprobe.main`）／`-20`（`r252_bp1_plan.plan`）／`-21`（`r252_bp1_qcheck.main`），本记录 `-22`（`R252`）。全 gate=PASS、raw_matches=0。

**BP1b（2026-09-25，提交 `7b548d84`）** —— (甲) 类补引号，4 行 / 2 章 / **新增 5 对引号**。批号走 §四 分批表，工具与表名仍为 `r252_bp1b_*`。**这是 665–724 坏父本带的第二批**（自然序 8），也是本卷第一个「半批近乎无活可做」的带 —— 而这是量出来的，不是读出来的。

1. **本带的证据面：父本 16,804 个引号对里，只有 20 对是台词。** 父本 `9252b142^`（与更早见证 `eee81640` 在本带逐章的引号数、冒号数完全相同）在 ch665–684 共 **16,804 对，过 `is_dialogue` 的仅 20 对（0.12%）**；父本的 `说：` 标记是 **2,825 处裸冒号 vs 42 处带引号** —— 引号全花在强调上。判据②（裸冒号是本书自己的写法）在卷外语料再确认一次：V1 10 ／ V2 6 ／ V3 154 ／ V4 **77** ／ V6 **1,217** ／ V7 1 处裸冒号与带引号台词并存。所以本半批不是「把剥离轮削掉的引号贴回去」，而是「父本证成的对、现值丢掉的那几处」—— 这是 §一 事实在本带的必然结果，不是本轮少做。

2. **20 对逐对回现值定位（`.claude/tmp/r252_bp1b_pairs.py`）：已带引号 5 ／ 裸着 4 ／ 已不在 11。** 定位法是「父本对首到行尾的盲键 → 在现值整章里取长唯一前缀 → 回行/列 → 引号态机判带引号还是裸着」。**已带引号 5**：ch679 父 L62／L66 三对、ch680 父 L13／L17 两对，都在已被后续轮次改写过的章上（ch680 是半带引号的章：L5–L45 裸冒号、L465 起带引号，两半统一属改写任务，不在本半批）。**裸着 4 里 2 处是假阳性**：ch667 父 L3 的 `。但0429碎片在赵大嘴的神经通路中`（内容**起于句号**）、ch683 父 L35 的 `"枪栓。"`（内容是枪栓，不是台词）—— 都是剥离轮 `。"` 残渣造成的奇偶错位对。**已不在 11 里 7 处同为强调误判**（`意识。`／`、赵大嘴。`／`爸爸。`／`发光。`×2／`谢谢。`／`关键词。`），另 **4 处是定位器的键长下限**（三字以下的键过不了 `range(len(tk),3,-1)`：`沈阿姨。` 尾对、`阿姨。`、`谢谢你。`、`叶文轩。谢谢你帮我照顾小鱼。`），逐处手工找回。

3. **补漏普查：1,069 对「台词形」被 `is_dialogue` 拒掉，全是强调残骸。** 反向扫（父本里内容收在句末标点、后一字不属标点的对）本带得 1,069 对，逐对读样本：`核心。`／`无法。`／`算不到。`／`玉兰花的。`／`：捕获工具。` —— 内容是散文碎片，成因是强调引号奇偶错位（`核心。"中` 之类）。结论：本带的台词证据面就是那 20 对，**不开新口子**（前七带一律这么做）。

4. **最终四处、五对。** ch673 L13（现值 `叶文轩。谢谢你帮我照顾，小鱼。` ← 父 L13 `沈知秋的声音："叶文轩。谢谢你帮我照顾小鱼。"`；同章 L25 已有带引号副本，故本行是剥离残句，归属语已被后续轮次删去，按 §八 不回填）、ch679 L70（父 L70 的 `"沈阿姨。"` 头尾两对）、ch679 L88（父 L88 首对 `"阿姨。"`）、ch679 L146（父 L146 首对 `"我选C。"`）。**同行的尾段未动**：`阿姨。我，帮你，…` 与 `我选C，带走记忆，带回现实。` 在父本里写成**错位强调引号**（`"阿姨"。，"我。"帮你"`／`"我"选"C。带走。"记忆"`），不成对，按判据③ 不动并登记（第 7 条 ②）。

5. **ch673 L13 顺带是 BP1a 的一处漏网，凭剥离提交自己的 diff 证明。** 该行的 `，`（`照顾，小鱼`）不是无据的逗号：`git show 9252b142 -- chapters/volume-5/chapter-673-polished.md` 写明 `-谢谢你"帮我"照顾。"小鱼"。` → `+谢谢你帮我照顾，小鱼。`，即剥离轮的 `。"` → `，` 就长在这一行上；而父本对的内容（`叶文轩。谢谢你帮我照顾小鱼。`）正是本行复位后的样子，故逗号随同一处编辑离场。**同一形态在同章 `0428备份吐出：叶文轩。谢谢你帮我照顾，小鱼。` 一行也有**，但那行没有台词对作依据，未动、登记（第 7 条 ④）。BP1a 的父本比对法看不见这两处 —— 它们的父本对应行是强调错位行，不是缺口头。

6. **新查出一个既存格式族：`\r\r\n` 双回车（本批只记录，未改）。** ch679 与 V5 另外九章（552／563／617／618／661／670／672／695／697）每行都以 `\r\r\n` 结尾，**HEAD 的 blob 里就是这样**，不是本轮或近几轮写坏的。后果只落在读法上：任何**通用换行**读法（`open()` 默认 `newline=None`，或 `read().replace("\r\n","\n")`）会把 `\r\r\n` 看成两个换行 —— ch679 的 L70／L88／L146 在那样的读法下报成 L139／L175／L291，本批第一版清单就是这么写错的，靠行尾字节普查纠正。**规矩**：本带的清单与锚点一律从盘的字节取行尾（引擎的锚点法本就不依赖行号），行号只作登记用。另注：这十章与**孤行引号族**（ch552／563／617／618／679）高度重叠，两族可能是同一支转换的产物，建议在孤行引号那一轮合并处理。

7. **登记（未动）**：① 孤行引号族 ch679 L123／L124、L131／L132（B5a 第 3 条已记；修它要把孤行引号并回上一行，行数会变，引擎不允许，属另一轮）；② 父本为错位强调引号、不成对的尾段（ch679 L88 尾、L146 尾 —— 候「父本权威回扫」轮按现场判读）；③ 全带裸冒号体约 420 处（判据②），含 ch679 的 `0429碎片传来信号：我选C。`；④ ch673 的 `0428备份吐出：叶文轩。谢谢你帮我照顾，小鱼。` 一处已证成的残渣逗号（第 5 条）；⑤ 父本无对的叙述行一律未动。

8. **验收**：1/2/3/4/6/8 过（CJK 逐章相等 2/2：ch673 3299、ch679 3036，前后不变；引号 ch673 40→42、ch679 8→16，每章偶数且无 `""`；L1+DEADLY 逐章不涨（ch673 各 1 处命中，前后相同）；4 处 `--verify` 从 HEAD 重放复现工作区；表内每行编辑前后的 CJK 序列相同，4 行全过）。本批另加两道自查：**行尾结构**（逐章 CR／LF／CRCRLF 计数与 HEAD 完全相同、行数不变、无 BOM）与**锚点唯一性**（ch673 的同一句在章内出现两次 —— 一次独立成段、一次尾随 `0428备份吐出：`，锚点因此向外长到唯一为止）。**第 5 条未达**：ch675 = 2998 CJK（本批未动；既存待补，与 V6-820=2962 同列）。**第 7 条未达**：本带 **4.19 → 4.27/千**（+5 对，全带 259 → 264 对）—— 与 (甲) 类前九批同结论。**第 9 条按作者指令跳过**（作者「不用读你直接继续改」）。

9. **工具**：`tools/r252_bp1b_sites.json`（4 行）+ `.claude/tmp/r252_bp1b_pairs.py`（父本对 → 现值定位，20 对的分母）+ `.claude/tmp/r252_bp1b_table.py`（表构建：行尾取字节、锚点窗口向外长到唯一、任一检查不过即 ABORT 不写盘）+ 只读普查 `.claude/tmp/r252_bp1b_colon.py`／`r252_bp1b_scope.py` + 复用引擎 `tools/fix_r252_b1a.py`（未改）。**由 `r252_bp1b_make.py` 从 B10b 派生的七件工具本批未用**：本带没有可批量推导的「缺失台词引号」，其中 `r252_bp1b_gen3.py` 另有一处可证的缺陷（`MARK.search(line, colon-12, colon)` 的 `endpos` 落在冒号之前，模式却以 `：` 收尾，永不命中），已由 `r252_bp1b_pairs.py` 取代。审计 `-23`（`r252_bp1b_make`）／`-24`（`r252_bp1b_colon`）／`-25`（`r252_bp1b_gen3`）／`-26` 与 `-27`（`r252_bp1b_scope`）／`-28`（`r252_bp1b_table`），本记录 `-29`（`R252`）。全 gate=PASS、raw_matches=0。

**BP2a（2026-09-25，提交 `51f1fa80`）** —— 标点半批，**73 行 / 16 章**（本带 20 章中 4 章无位点），只做 §三 授权的第 2 类编辑。批号走 §四 分批表，工具与表名仍为 `r252_bp2_*`：`bp2` = **坏父本第 2 带**。**这是 665–724 坏父本带的第三批**（自然序 9）。

1. **工具由 BP1 整族派生，每一处替换都带期望命中数。** `.claude/tmp/r252_bp2_make.py` 从 BP1a 的那几件工具生成 BP2 的一族（`r252_bp1`→`r252_bp2`、`BP1`→`BP2`、`665-684`→`685-704`、`range(665, 685)`→`range(685, 705)` …），替换后做泄漏扫描：规划器里不得再出现 `r252_bp1`／`BP1`／`r252_b10`／`B10`／`665`／`684`，也不得残留 BP1 的三条手行。两处块**写死清空**：`HAND`／`HAND_PARENT`（一个带的手行不能带另一个带的键，否则「手行未触发即 ABORT」这条检查会在范围外的行上触发）与 `handgen` 的 `OVERRIDE`／`EXPECT`。派生时另修掉一处会**量出空洞结论**的缺陷：BP1 是**就地**放宽 `repair_sites` 的，派生的价格探针会把放宽答案算两遍、报出假 `差 0 行`；`make` 因此从放宽函数**反建**一个 strict 变体（`^(，?)。"`），比对式一并改写（第一次用 4 空格缩进锚不上 8 空格的真行，断言 `relax comparison -> 0` 拦住了）。

2. **提案 73 条 / 16 章，全以父本 `9252b142^` 为证。** 规则行 **49**（乙4 22 / 乙6 6 / 乙7 5 / 乙3 4 / 乙2 4 / 乙5 4 / 乙9 3 / 乙8 1）+ 手行 **24**。分布：ch686 9、ch695 9、ch688 6、ch690 6、ch691 6、ch693 6、ch694 5、ch697 5、ch692 4、ch696 4、ch702 4、ch698 2、ch699 2、ch700 2、ch701 2、ch689 1；**ch685／687／703／704 四章无位点** —— 其中 ch685、ch687 是引号齐备的两章，本带 500 个引号全落在这两章（194／306），其余 18 章一个引号也没有。

3. **放宽价格：本带 +4 行、0 处回归。** 与 BP1 同一处放宽（缺口模式由 `^(，?)。"` 放宽为 `^(，?)"?。"` —— 坏父本带的剥离输入形状是 `字"。"字`，强调引号收在句号**之前**，句号不是缺口首字）。`.claude/tmp/r252_bp2_relax.py` 逐行比对严格／放宽两答案：**差 4 行** —— ch686 L59、ch690 L29、ch690 L85、ch695 L31，四行逐行读过（`爸爸在这里叶文轩的意识，在说。朵朵，爸爸在这里，爸爸很快，回来。` → `…意识在说。…爸爸很快回来。`；`我爸说，过赵大嘴的声音，在抖，系统算不到，我。` → `我爸说过。赵大嘴的声音在抖系统算不到我。`；四条 `X/keep/add` 见证据文件）。放宽不改变 `open_at` 的落点，其余代码一字未动。**探针在落盘后自然归零**（残渣逗号已修，严格与放宽同答），故证据文件是对 `HEAD~1` 重放留存的。

4. **24 行落在「整行对齐」的盲区，用章级扫描读出来写进 `HAND`。** 分母与 551–750 前几带同量级：整行 **0** / 片段 **24** / 跨行 **0** / 无逗号 **2,552** / 在表内 **49** / 无位点 **35** / 无法定位 not-found **1,331**、ambiguous **6**。24 行**不是手打清单**：`.claude/tmp/r252_bp2_handgen.py` 用同一套函数把结果写进 `HAND`／`HAND_PARENT`（marker 之间，24 行 / 24 条父本依据），一行一注（X 序列写进注释）；写入前断言每行 CJK 序列不变、且不得写出 `。，`／`，。`／`。。`。写入后另做一次比对：`HAND` 的 24 行与 resolver 打印的 24 条 `提` 行**逐字相同**。

5. **引号行 35 条，一处不欠。** 章级解析器对带引号的行一律跳过（(甲) 类留给 BP2b 那一半），但残渣规则读的是**盲键**（引号、逗号、句号都在键外），带引号的行同样可查。`.claude/tmp/r252_bp2_qcheck.py` 对全部 35 行跑同两段式，输出 **`引号行 35 条，其中可修 0 条`** —— 与 BP1a 的 26 条／可修 0 同形。

6. **双回车族：本带是第一支「记录在案之后」仍要编辑 `\r\r\n` 章的带。** BP1b 第 6 条记下的十章里，ch695／ch697 正落在 685–704，本带在它们身上各有 9 行／5 行（含手行各 1）。清单与锚点的行尾一律从盘的字节取，落盘后逐章字节普查：**CR=2×LF、CRCRLF=LF，与 HEAD 的 blob 逐项相同**（该族在 HEAD 里就是这样，不是本地漂移）；`--verify` 的 CR 旗标一致检查也照过。BP1b 记下的那个读法坑本批又踩到一次 —— 诊断打印用通用换行读法时行号会翻倍，靠字节计数纠正。

7. **零处由读句子改判**（`OVERRIDE = {}`）：本带没有像 ch672 L13 那样需要现场定夺的行，73 条全部按规则／resolver 的合成结果落盘。

8. **登记（未动）**：① **缺失逗号族（本批新见）**：ch686 L25／ch688 L41 —— 父本 `一个"。"窗口"在"他"的"意识。"中"。"打开"了。窗口"中"有。"一个"。"画面"，朵朵"在"画室。"中"。` 在 `画面` 后有**一个逗号**，现值既无逗号也无句号（合成结果 `窗口中有一个画面朵朵在画室中。`）；这不属引擎的两条内部分支，也不属本轮两类编辑，登记待「父本权威回扫」轮。② **游移句号**：`光点在。组成` 八处（ch691 L95／L123、ch692 L93、ch693 L73、ch694 L53、ch695 L47、ch696 L27、ch698 L33）与 `在。追` 一处（ch693 L33）—— 后来的某一轮把剥离轮的逗号改回了句号、句号仍留在关键词**之前**，同 B2a 第 4 条①。③ **句号链噪声**：ch695 L31 的 `在抖系统算不到我。` 按 B6a 第 3 条只删残渣逗号、不补句号。④ 无法定位 not-found 1,331 / ambiguous 6 / 无位点 35 行（即引号行）。⑤ 父本无对的叙述行一律未动。

9. **验收**：1/2/3/4/6/8 过（CJK 逐章相等 **20/20**，60,397 → 60,397；引号 500 → 500，每章偶数且无 `""`；L1+DEADLY 逐章不涨（16 章 0 处上涨）；73 处 `--verify` 从 HEAD 重放复现工作区且 CR 旗标一致；行数不变、无 BOM；表内每行编辑前后的 CJK 序列相同，73 行全过）。**第 7 条未达**：本带 **4.14 → 4.14/千**（+0 对）—— 与标点半批前八批同结论：标点不动密度。**第 9 条按作者指令跳过**（作者「不用读你直接继续改」）。本带 20 章全部 ≥3000 CJK（最短 ch704 = 3,001、ch685 = 3,076）。验收明细存 `.claude/tmp/r252_bp2_dry.txt`（`−／+` 逐行）与 `.claude/tmp/r252_bp2_relax.txt`。

10. **工具**：`tools/r252_bp2_sites.json`（73 行 = 49 规则 + 24 手）+ `.claude/tmp/r252_bp2_make.py`（整族派生：替换带断言 + 泄漏扫描 + strict 反建）+ `.claude/tmp/r252_bp2_plan.py`（继承放宽一处 + `HAND`／`HAND_PARENT` 24 行）+ `.claude/tmp/r252_bp2_relax.py`（放宽价格）+ `.claude/tmp/r252_bp2_gapscan.py`（章级扫描）+ `.claude/tmp/r252_bp2_gapres.py`（章级解析）+ `.claude/tmp/r252_bp2_handgen.py`（手行写入）+ `.claude/tmp/r252_bp2_qcheck.py`（引号行复查）+ 复用 `tools/fix_r252_b1a.py`（引擎未改）。派生了但本批未用：`r252_bp2_handprobe.py`／`r252_bp2_probe.py`（同 BP1b 那七件）。审计 `-30`（`r252_bp2_make.main`）／`-31`（`r252_bp2_plan.plan`）／`-32`（`r252_bp2_handgen.main`）／`-33`（`r252_bp2_gapscan.main`）／`-34`（`r252_bp2_gapres.main`）／`-35`（`r252_bp2_relax.main`）／`-36`（`r252_bp2_qcheck.main`）／`-37`（`fix_r252_b1a.transform`），本记录 `-38`（`R252`）。全 gate=PASS、raw_matches=0。



**BP2b（2026-09-25，提交 `d0707774`）** —— (甲) 半批，**2 行 / 2 对 / 2 章**。**这是 665–724 坏父本带的第四批**（自然序 10）。批号沿用 `bp2`，工具名 `r252_bp2b_*`。

1. **分母 26 对，逐条判读后只剩 2 对。** `.claude/tmp/r252_bp2b_pairs.py`（由 BP1b 的 `pairs.py` 派生）输出 **父本台词对：已不在 2 ／ 带引号 2 ／ 裸着 22，合计 26**。已不在的 2 条是 ch687 L7 的 `弱点。`／`能力。`（被后轮改写，父本依据已不成立）；带引号的 2 条是 ch685 L7、ch687 L73（后轮已复位，未动）。

2. **22 条「裸着」逐行判读：20 条是强调对位，不是丢掉的台词。** 判据一张表：① **17 条是行中的两字名词片段** —— `弱点。`（ch689 L21／L55、ch696 L39）、`通道。`（ch690 L67）、`房间。`（ch690 L99）、`发热。`（ch691 L11）、`结构。`（ch699 L17 两处、ch701 L11）、`出口。`（ch699 L17）、`碰撞。`（ch700 L70）、`微笑。`与`拥抱。`（ch703 L11）、`布局。`（ch704 L5）、`第一层。`／`第二层。`／`第三层。`（ch704 L7）。父本对应处是强调对位（`一个"。"微笑"。一个"。"眼泪"。`、`，"弱点`、`很多。房间。很多，通道`），走的是 `is_dialogue` 的**收句**分支（内容以句号收尾且 CJK≥2），不是**行首**分支 —— 加引号等于凭空造一个说话人；② **3 条 `我爸。`**（ch695 L49、ch697 L37、ch702 L21）虽在行首，但父本里是强调链成员（`我爸"。"在"记录"什么。`），内容是名词不是句子；③ 其中 ch699 L17、ch701 L11 等行**本行一个引号也没有**，更无从谈起「丢了引号」。

3. **真位点两处，同一句、同一模板。** ch699 L41 与 ch701 L27 的父本行都是 `"我同意。"赵大嘴"的"声音。"很"。"平"。但"清晰。"的"。赵大嘴"的"声音。"在"。"抖"。…`；两行都以**第一人称应答**开行，后接归属语（`赵大嘴的那声音平。`／`赵大嘴的语气很平。`），与 ch687 L73 那对已由后轮复位的 `"先面对维护派。"叶文轩说。` 同形 —— 都属 `is_dialogue` 的**行首**分支。

4. **剥离提交自身的 diff 是最直接的证据，语料三处互证。** `git show 9252b142 -- chapters/volume-5/chapter-699-polished.md` 里该行是一步写成的：`-"我同意。"赵大嘴"的"声音…` → `+我同意。赵大嘴的声音，很平。…`（引号与强调链在同一处编辑里一起消失）。语料：`outline-volume-5.md:496`（`赵大嘴没有犹豫。"我同意。"`）、ch580 L59、ch891 L177、ch893 L121（`"我同意。"赵大嘴说。`）。

5. **两章在编辑前一个引号也没有**（ch699 0 → 2、ch701 0 → 2）：表内新增对与章内引号增量相等，无需对账。

6. **工具由 BP1b 整族派生，两处带块在裸数字替换之前写死。** `.claude/tmp/r252_bp2b_make.py`：`r252_bp1b`→`r252_bp2b`、`BP1b`→`BP2b`、`ch665-684`→`ch685-704`、`range(665, 685)`→`range(685, 705)` 等，每处替换带期望命中数，替换后泄漏扫描（`r252_bp1b`／`BP1b`／`665`／`684`／`731`／`750` 不得残留），最后断言四件派生文件都含 `range(685, 705)`。两处块必须先换：① `colon.py` 的 `LIVE` —— BP1b 的排除元组 `(665, 666, 668, 681, 684)` 若走裸数字替换会变成 `(685, 686, 688, 691, 704)`，无理由排除五章，故整行重写为整带 `list(range(685, 705))`（普查逐行跳过引号行，不需要章级排除）；② `scope.py` 的说明串 —— 本带被后轮重写过的章是 **685 与 687**（实测：全带 500 个引号全在这两章，194／306，其余 18 章为 0）。

7. **表与引擎。** `.claude/tmp/r252_bp2b_table.py`（BP1b 表构建的同一套：表里的 `parent` 见证串与 `git show 9252b142^:<章>` 的对应行**逐字比对通过 2/2**；行尾取字节、锚点窗口向外长到唯一、任一检查不过即 ABORT 不写盘）→ `tools/r252_bp2b_sites.json`（2 行，均 `kind=甲`）→ 复用 `tools/fix_r252_b1a.py`（引擎未改）`--dry`（−／+ 与表逐字一致）→ `--apply` → `--verify`（从 HEAD 重放复现工作区）。两处锚点在各自章内**只出现一次**，窗口未生长；两行行尾均 `\r\n`，两章都不属 `\r\r\n` 族（CRLF=LF、无孤立 CR／LF）。

8. **登记（未动）**：① 20 条强调对位（第 2 条的判据表）—— 与 BP1b 的 7 条同类，属「不开新口子」；② `已不在` 2 条（ch687 L7）；③ 三条行首 `我爸。`（ch695 L49、ch697 L37、ch702 L21）的父本强调痕迹要不要在这三行留痕，留「父本权威回扫」轮按现场判读；④ ch699 L41 与 ch701 L27 同句在两章里的**词级差异**（`赵大嘴的那声音平。` vs `赵大嘴的语气很平。`）是后轮改写，不属本轮两类编辑；⑤ 既存 de-AI 命中：ch697／ch698 各 1 处 L1+DEADLY（前后相同，本批未动）。

9. **验收**：1/2/3/4/6/8 过 —— CJK 逐章相等 **20/20**（60,397 → 60,397）；引号 500 → 504，每章偶数且无 `""`；L1+DEADLY 逐章不涨；2 处 `--verify` 从 HEAD 重放复现工作区；行数不变、无 BOM、逐章 CR／LF 计数与 HEAD 相同；表内两行的新增对**全部通过 `is_dialogue`**（表内新增对 2 = 章内引号增量 2）。**第 7 条未达**：本带 **4.14 → 4.17/千**（+2 对，全带 250 → 252 对）—— 与 (甲) 类前十批同结论。**第 9 条按作者指令跳过**（作者「不用读你直接继续改」）。验收明细见 `.claude/tmp/r252_bp2b_accept.py` 的输出（20 章表 + `表内新增引号对 2；is_dialogue 未通过 0`）。

10. **工具**：`tools/r252_bp2b_sites.json`（2 行）+ `.claude/tmp/r252_bp2b_make.py`（整族派生）+ `.claude/tmp/r252_bp2b_pairs.py`（分母 26 对）+ `.claude/tmp/r252_bp2b_colon.py`／`r252_bp2b_scope.py`（只读普查）+ `.claude/tmp/r252_bp2b_accept.py`（验收）+ `.claude/tmp/r252_bp2b_table.py`（表构建）+ 复用 `tools/fix_r252_b1a.py`（引擎未改）。审计 `-39`（`r252_bp2b_make.main`）／`-40`（`r252_bp2b_pairs.main`）／`-41`（`r252_bp2b_colon.main`）／`-42`（`r252_bp2b_scope.main`）／`-43`（`r252_bp2b_table.main`），本记录 `-44`（`R252`）。全 gate=PASS、raw_matches=0。

**BP3a（2026-09-25，提交 `e5e3db48`）** —— 标点半批，**96 行 / 10 章**（本带 20 章中 10 章无位点），只做 §三 授权的第 2 类编辑。批号走 §四 分批表，工具与表名 `r252_bp3_*`：`bp3` = **坏父本第 3 带**（705–724；坏父本 705–707／711／719–721 七章，其余 13 章常规）。**这是 665–724 坏父本带的第五批**（自然序 11），也是**第一个混合带** —— 前两带整带坏父本，放宽价格不能沿用，本带重新量过（第 3 条）。

1. **工具由 BP2 整族派生：带块先换，再替换裸数字。** `.claude/tmp/r252_bp3_make.py` 从 BP2 那一族生成 BP3 一族（`r252_bp2`→`r252_bp3`、`BP2`→`BP3`、`685-704`→`705-724`、`range(685, 705)`→`range(705, 725)` 等），每处替换带期望命中数；替换后泄漏扫描（`r252_bp1`／`BP1`／`r252_bp2`／`BP2`／`r252_b10`／`B10`／`665`／`684`／`685`／`704` 不得残留），最后按**每件工具自己写带的字面量**逐文件断言（规划器 `["705-724"]`、gapscan `["705", "724"]`、gapres／qcheck／handprobe／handgen `range(705, 725)`、relax `ch705-724`）。第一版断言写成「每个文件都得含 `"705"` 与 `"724"`」，被自己的检查拦住并中止（四件工具用的是 `range` 形式），改成逐文件期望值后重跑通过 —— 与各带同规：**派生工具的任何断言失败一律不写盘**。两处块写死清空：`HAND`／`HAND_PARENT` 与 handgen 的 `OVERRIDE`／`EXPECT`。放宽价格探针多一道 `must_not`（`r252_bp2`／`r252_bp1`／`685`／`704`）：它在近旁留着 BP1／BP2 的对照值，而**大写 `BP1` 的散文说明要留**（讲的是 strict 变体从哪来），故 must_not 只列模块名与带号，另断言 `repair_sites_strict(`／`repair_sites_relaxed(` 两个定义与两处调用形式都在。

2. **提案 96 条 / 10 章：规则 83（乙1 58／乙2 12／乙3 5／乙4 1／乙5 1／乙6 1／乙7 3／乙8 2）+ 手行 13。** 分布 ch714 22、ch713 15、ch708 13、ch709 11、ch715 11、ch706 8、ch718 7、ch705 5、ch716 2、ch724 2；**707／710／711／712／717／719／720／721／722／723 十章无位点**。坏父本七章里只有 705（5 行）与 706（8 行）有位点，另外五章为空 —— 与前两带「坏父本带位点密」的印象相反，本带 96 行里 83 行落在常规章上。凡改动一律以父本 `9252b142^` 为证（表内 `parent` 列即见证串）。

3. **放宽价格：本带差 1 行（ch705 L35），0 处回归。** BP1 是 +9 行、BP2 是 +4 行，两支都整带坏父本；本带 7 章坏父本 + 13 章常规，价格随之落到 1 行 —— 这是「混合带必须重测」那句话的第一次实测，证据存 `.claude/tmp/r252_bp3_relax.txt`。该行（8 个位点，其中 1 处 `add_period`）已作为乙8 行落表：现值 `…第三个人，在反应，他的设备，在闪烁…设备熄灭。了。设备，坏了。` → `…第三个人在反应，他的设备在闪烁…设备熄灭。了。设备坏了。`（父本 `设备"。"坏"了。` 证成的是**残渣逗号**，句号已在位，不补）。探针在落盘后自然归零（残渣修完，严格与放宽同答），故证据文件是对编辑前状态留存的。

4. **13 行落在「整行对齐」的盲区，用章级扫描读出来写进 `HAND`。** 分母与 685–704 同量级：整行 **0** / 片段 **13** / 跨行 **0** / 无位点 **350** / 无逗号 **2,701** / 在表内 **83**（落表前）→ **96**（落表后）/ 无法定位 not-found **726**、ambiguous **31**。13 行**不是手打清单**：`.claude/tmp/r252_bp3_handgen.py` 用同一套函数把结果写进 `HAND`／`HAND_PARENT`（marker 之间，一行一注，X 序列写进注释），写入前断言每行 CJK 序列不变、且不得写出 `。，`／`，。`／`。。`；写入后另做一次比对：`HAND` 的 13 行与 resolver 打印的 13 条 `提` 行**逐字相同**。分布 ch706 ×5、ch718 ×6、ch708 ×1、ch713 ×1。

5. **零处由读句子改判**（`OVERRIDE = {}`）：13 条合成行只有两种形态 —— 「删残渣逗号」与「删残渣逗号 + 按父本补句号」—— 落盘前逐行读过；`在变薄。`／`在恢复。`／`在增强。` 这类连写全库各卷都不带杂逗号（与 BP2a 第 7 条同一理由），故 13 行全部按 resolver 的合成结果落盘。

6. **引号行 180 条，一处不欠。** 章级解析器对带引号的行一律跳过（(甲) 类留给 BP3b 半批），但残渣规则读的是盲键（引号、逗号、句号都在键外）。`.claude/tmp/r252_bp3_qcheck.py` 对全带 480 条引号行中**含逗号的 180 条**跑同两段式，输出 **`引号行 180 条，其中可修 0 条`** —— 与 BP1a 的 26／0、BP2a 的 35／0 同形。

7. **引擎缺陷一处，本批修掉并留档。** `chapter_sites` 里「父本台词对越过本行末尾」的守卫，原本放在**读本行索引之后**：`k` 数的是**父本** token，`t + k` 会越出本行自己的 `lidx`，`line[lidx[t + k] + 1]` 即 `IndexError`（`r252_bp3_gapres.py:108`）。本带 `qcheck` 首跑即崩在这个行集上（gapres 自己的行集未触发 —— 它 01:17 那次普查是完整的）。修法：把守卫上移到读 span 之前（越过末尾即计入 `cross` 并跳过）。**影响面**：只可能改变章级普查里 `跨行`／`片段` 的归类，不改变任何合成行 —— 修复后重跑 gapres，除 13 条片段行按新表转入「在表内」（片段 **13→0**、在表内 **83→96**）外，输出逐字不变，96 行表未动。**BP1／BP2 两代工具副本仍带此缺陷**（其带内行集未触发），已闭轮不再回改，登记待「父本权威回扫」；BP4 起的派生自动继承修复。

8. **行尾第二变体：LF-only 章。** 本带 ch711／716／720／722／723 五章在盘上是**纯 LF**（CR=0），与 BP1b 第 6 条记的 `\r\r\n` 双回车族不同，但同属「行尾一律从本行字节取」这一条规矩：96 行锚点 94 行用 `\r\n`、2 行（ch716）用 `\n`。落盘后逐章字节普查：20 章 CR／LF 计数与 HEAD 的 blob 逐项相同、行数不变；`--verify` 的 CR 旗标一致检查照过。

9. **验收**：1/2/3/4/6/8 过（CJK 逐章相等 **20/20**，67,922 → 67,922；引号 1,248 → 1,248，每章偶数且无 `""` —— 本批为标点半批，不动引号；L1+DEADLY 逐章不涨；行数不变、无 BOM、逐章 CR／LF 计数与 HEAD 相同；96 处 `--verify` 从 HEAD 重放复现工作区；表内每行编辑前后的 CJK 序列相同，96 行全过）。**第 5 条**：本带 20 章全部 ≥3000 CJK（最短 ch706 = 3,012，次短 ch717 = 3,018）。**第 7 条未达**：本带 **9.19 → 9.19/千**（+0 对，全带 624 → 624 对）—— 与标点半批各批同结论：标点不动密度。本带密度远高于 685–704 的 4.14/千，缘于 ch720（266 个引号）／ch711（246）／ch721（222）三章引号集中。**第 9 条按作者指令跳过**（作者「不用读你直接继续改」）。验收明细存 `.claude/tmp/r252_bp3_dry.txt`（`−／+` 逐行）与 `.claude/tmp/r252_bp3_relax.txt`。

10. **工具**：`tools/r252_bp3_sites.json`（96 行 = 83 规则 + 13 手）+ `.claude/tmp/r252_bp3_make.py`（整族派生：替换带断言 + 泄漏扫描 + relax 探针 must_not）+ `.claude/tmp/r252_bp3_plan.py`（继承放宽一处 + `HAND`／`HAND_PARENT` 13 行）+ `.claude/tmp/r252_bp3_relax.py`（放宽价格）+ `.claude/tmp/r252_bp3_gapscan.py`（章级扫描）+ `.claude/tmp/r252_bp3_gapres.py`（章级解析，含第 7 条的修复）+ `.claude/tmp/r252_bp3_handgen.py`（手行写入）+ `.claude/tmp/r252_bp3_qcheck.py`（引号行复查）+ 复用 `tools/fix_r252_b1a.py`（引擎未改）。派生了但本批未用：`r252_bp3_handprobe.py`／`r252_bp3_probe.py`（同 BP1b 那七件）。审计 `-45`（`r252_bp3_make.main`）／`-46`（`r252_bp3_plan.plan`）／`-47`（`r252_bp3_handgen.main`）／`-48`（`r252_bp3_gapscan.main`）／`-49`（`r252_bp3_gapres.main`）／`-50`（`r252_bp3_relax.main`）／`-51`（`r252_bp3_qcheck.main`）／`-52`（`r252_bp3_gapres.chapter_sites`，第 7 条的修复），本记录 `-53`（`R252`）。全 gate=PASS、raw_matches=0。

11. **更正 BP2b 记录第 9 条的密度单位。** BP2b 写的「本带 **8.28 → 8.34/千**」是**引号字符**每千（500 → 504 个引号），而本计划其余各条（BP1a 4.18／BP1b 4.27／BP2a 4.14／本条 9.19）一律用**引号对**每千（250 → 252 对）。同一组数换到统一单位是 **4.14 → 4.17/千**，本次一并改回；括号里的「全带 250 → 252 对」原文不动。
