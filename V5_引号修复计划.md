# V5（ch551–750）对话引号修复计划

> 2026-09-24 摸底后定稿。承接 `tools/fix_r251_quote_restore.py`（阶段 1 试点已 apply 于 ch745/650/654，未提交）。
> 本文件是**工作计划**，不含正文改动。开工前每一步的验收口径见第四节。

## 一、现状：四条实测事实

1. **引号被剥是损坏，不是风格。** `9252b142`（2026-08-22「V5 mechanical deslop complete」）一次把 V5 的 ASCII 引号从 182,077 削到 2,747（−98.5%），提交信息自己写着 `Quote-striipping`。200 章里 196 章受影响、111 章归零。

2. **父本不能整卷回退。** `9252b142^` 的 90,191 个引号对里，只有 20,206 对（22.4%）是对话，其余 69,985 对（77.6%）是术语/强调引号（`感觉到`/`编译`/`结构`/`沉睡`）——正是 R238 从 V6/V7 清掉的那类 AI 味。整卷回退 = 撤销 R238。

3. **父本自身分两类，必须分开对待。**

   | 类 | 判据 | 章数 | 章号 |
   |---|---|---|---|
   | 坏父本 | 每行引号 > 10 | **46** | 665、666、668–707（缺 667）、711、719、720、721 |
   | 可用父本 | 每行引号 ≤ 10 | 154 | 其余 |

   坏父本带的原始形态是**强调引号灾难**，每个短语都被引号包住，句号被塞进引号里：

   ```
   父  叶文轩能"感觉到"0428备份在"发光"，0428备份在"说"：开始。"吧"。
   父  白色空间在。"变"。0428备份的"粉色纹路"在"变亮"，是"逐渐。"的"。"像"一个"灯"在。"慢慢"。"调亮"。
   现  白色空间在变0428备份的粉色纹路在变亮，是逐渐，的一个灯在慢慢调亮。
   ```

   现值坏在**两层**：引号被剥 + `。"` 被换成 `，`（族 B）。这 46 章的父本不能当机械对齐的参考，但**仍是"原文想说什么"的唯一见证**。

4. **坏的边界不止这 46 章。** 全卷「引号之外的引述信号」（`说：` 与紧邻引述结构）共 **3,842 处 / 199 章**，分布 30+ 有 39 章、15–29 有 70 章、5–14 有 71 章、1–4 有 19 章、0 有 1 章——这是工作量的**下界**（不含无引述的短台词如 `"嗯。"`）。

### 一·补（2026-09-24，R251 提交后实测，**推翻上面第 4 条的排批依据**）

5. **机械通道已经见底。** 对齐行上父本共带 **11,425 对**引号，`is_dialogue` 只认 **2,465 对（21.6%）**——与全卷「父本对话占 22.4%」吻合，说明分类器是准的，是父本里 78% 本来就是术语/强调引号。R251 已拿走 **2,286 对（92.7%）**。剩下的 8,960 对里，真正属于「分句台词上半」（内容以 `，` 收尾且紧跟引述结构，如 `"大嘴，"叶文轩再次开口，…`）的只有 **40 对 / 19 章**。**放宽闸门③（行内已含引号则跳过）净增 0 对**——实测那 2,429 对本来就在（2,286 是 R251 补的，143 是原文就有）。即：再把 R251 调松也榨不出东西了。

6. **父本只覆盖 23% 的行，对它 77% 的正文不是「原文」而是「见证」。** 全卷 43,787 行父本里只有 10,115 行能与现值对齐。`ch629` 是标本：现值 120 行 vs 父本 **681 行**，**对齐 0 行**；因为该章被后续轮次整章重写过（同类的 `b40b715b`＝R213 对 ch665 一次换了 276 行，把 deslop 的逗号碎片重写成通顺散文、台词改用 `「」`）。**凡是重写过的章，R251 那种「照父本重放」在原理上就不可能命中。**

7. **R252 的真实病灶是两类，且都不能照父本回填。**

   - **(甲) 有引述结构、却一个引号都没有的行。** 粗测 **954 行 / 180 章**（含 `大嘴没有回答`、`不能现在问` 这类假阳性，真数约一半）。标本 ch551：`大嘴，叶文轩再次开口，转身走回去。"你胸口那块碎片。它怎么了？"`——**半行有引号、半行没有**（父本两半都有）。这类是读者一眼能看见的伤。
   - **(乙) 被机械剥走的标点。** 剥引号那轮的顺序是**先 `。"` → `，`、再删引号**，所以现值的 `，` 正是那两轮的残渣。**可证**位置 **1,442 处 / 119 章**（父本 `。"` ↔ 现值 `，`，重灾 ch683 95 / ch682 50 / ch587 39 / ch631 38）。另有 1,823 处父本有 `。` 而现值是 `，` 的差异**不可证**——多为后续轮次**有意**的逗号链，**勿动**。
   - ⚠️ **两类都不能信父本。** 见 ch556 L54：父本自己写作 `石材中有维护派预置的。"记忆提取模块"。`——**句号被塞进了引号里**，父本也是坏的。正解是读句子定夺（此处应删逗号：`预置的记忆提取模块。`）。**父本给的是「原文想说什么」，不是「原文是什么」。**

8. **「坏父本 46 章」这个标签对排工作量没有意义。** 它衡量的是父本自己的强调引号密度，与现值还缺多少引号无关——所以第 4 条按「引述信号」排批也要降级。按第 7 条实测，缺陷密度 top20 是：683、682、587、631、635、705、714、570、568、633、691、695、634、654、692、632、652、690、588、591——**横跨 551–750，不是集中在最坏带**。故 R252 改按**自然序**分批（下表不变，理由改掉）。

9. **`「」` 不是台词，是标签。** V5 现值 `「` 只 440 个、140/200 章为 0；它是 R213 那类轮次给**模块名/显示文本**加的（`「锚点B抑制协议·执行层」`、`「三个结局，请选择」`）。**台词一律用 ASCII `"`，不要改用 `「」`**（V4 的 2,321 个 `「」` 是 V4 自己的事）。

## 二、目标口径

| 指标 | 现值 | 目标 |
|---|---|---|
| V5 CJK | 701,166 | 不变（引号与标点非 CJK） |
| ASCII 引号数 | 2,901 | 14,023–19,632 |
| 密度 | **4.1/千** | **20–28/千** |
| 对话引号对数 | 1,450 | 7,011–9,816 |
| 术语/强调引号对 | ~0 | 保持 ~0 |

参照带：V1 21.2、V2 22.1、V3 27.7、V6 24.3（V4 11.9 也偏低，是另一轮的事；V7 57.4 偏高）。

## 三、方法（三段）

### R251 阶段 1 —— 机械还原（全卷，一次提交）

`tools/fix_r251_quote_restore.py` 现有规则（只认精确对齐 `P == C` + 一对一映射 + 逐行自查）在**全卷**可还原 **1,751 对**（父本对话的 8.7%），把 V5 从 4.1 拉到 **9.1/千**。工具 fail-closed：`suspects > 0` 时拒绝写盘。

这一步单独做**不值得当终点**（会造出「40% 的章半有引号、60% 的章没有」），但作为**统一基线**值得——它严格更接近目标，且是纯机械、可审计、可重放。

### R251b —— 试验（只针对 46 章坏父本带，有 go/no-go）

阶段 1 的现行规则有**两个已知漏洞**，修掉它们可能把坏父本带从「全人工」变成「半自动」：

- **判据漏洞**：`is_dialogue` 现在把「内容含句号且 ≤40 字」一律当对话，于是 `"逐渐。"`、`"纹路。"`、`"的。"` 全被判成对话。更好的判据是**行位置**——整行（近乎）就是引号内容、或有紧邻引述、或长于 12 字，才算对话。
- **对齐漏洞**：`。"` → `，` 那批行 `P != C` 被直接跳过，但它们其实**只差标点**。把 `，。：！？、` 归一后比对（`norm(P) == norm(C)`），ch680 那类行就能对上。

**go/no-go**：在 ch665–707 上跑 dry，若该带还原率从 <2% 提到 ≥25%，则并入阶段 1 全卷跑；否则该带直接进阶段 2 全人工。**这是代码变更，动工前必过 legacy impact audit。**

### R252 阶段 2 —— 逐章补完（10 批）

逐章对照「父本（去引号）」与「现值」，只做两类编辑：

1. **加对话引号**；
2. **修复被剥离吃掉的标点**（`，中` / `，区域` 这类把逗号还原成句号或删掉）。

**硬约束：不回填父本被有意删掉的词。** 隐喻简化、`目光→视线`、`愣→怔`、`——→，` 都是既定简化（R238/V5 deslop 有意为之），回填等于撤销那些轮次。本阶段只补**引号与标点**。

每章产出一张**改动清单**（行号 / 修前 / 修后 / 依据：父本行 or 无父本依据），存 `.claude/tmp/`，供作者抽查。

## 四、分批表

| 批 | 章号 | 章数 | 说明 |
|---|---|---|---|
| R251 阶段 1 | 551–750 | 200 | 机械，一次提交；试点 3 章并入 |
| B1 | 665–684 | 20 | 坏父本带 |
| B2 | 685–704 | 20 | 坏父本带 |
| B3 | 705–724 | 20 | 坏父本 705–707、711、719–721（7 章）+ 常规 13 章 |
| B4 | 551–570 | 20 | |
| B5 | 571–590 | 20 | |
| B6 | 591–610 | 20 | |
| B7 | 611–630 | 20 | |
| B8 | 631–650 | 20 | 含试点 ch650 |
| B9 | 651–664 + 725–730 | 20 | 含试点 ch654 |
| B10 | 731–750 | 20 | 含试点 ch745 |

**按自然序扫（551 → 750），先打上表 B1–B3 只是沿用编号。** 原定「先打 665–724 最坏带」的理由（第 4 条）已被一·补第 8 条推翻：缺陷密度横跨全卷，最坏带并不更重。自然序的好处是**章内上下文连续**（同一批里人物状态、伏笔位置前后接得上），读句子定夺时误判率更低。

## 五、每批验收口径

一批 = 20 章，全部满足才算过：

1. **引号配对**：该批每章 `"` 计数为偶数；无 `""`；无跨行未闭合引号。
2. **无强调引号**：该批每章所有新增引号对通过 `is_dialogue` 判定，被误判的强调引号对 = 0。
3. **无错位引号**：无「引号插在半句中间」（R251 的 self-check + suspects 计数 = 0）。
4. **CJK 不动**：该批每章 CJK 计数与修前**逐章相等**（引号与标点非 CJK）。
5. **字数硬线**：该批每章 ≥3000 CJK。
6. **AI 味不涨**：该批每章 `polish_pipeline.py` 的 L1/DEADLY 命中数不增长。
7. **密度到位**：该批每章引号密度落在 **18–28/千**，V5 整体向 20–28 收。
8. **字节可重放**：`--verify` 从 `git show HEAD:<file>` 的字节重放该批全部编辑，**逐字节相同**（仅允许 EOL 差异——这是唯一能同时证明「只有预期处变了」与「CRLF/LF、无 BOM 未被扰动」的检查）。
9. **作者抽读**：每批抽 2–3 章通读，确认台词读起来是台词。

## 六、交付、回滚、进度登记

- **一批一提交**，英文里程碑信息 + 归属行，例如 `R252 B1: V5 dialogue quotes ch665-684 (N pairs, +N marks)`。
- **回滚 = `git revert` 该批那一个提交**，不影响其他批。
- 每批完成后在本文件底部追加一行进度（批号 / 章数 / 新增对数 / 该批密度 / 验收结果）。
- 全轮结束后复查：V5 密度、奇数引号文件数、术语引号对数、CJK 总量、L1/DEADLY 全库增量。

## 七、待作者拍板

1. **R251b 试验做不做？** 建议做——上限是把 46 章带从全人工变半自动；下限是白跑一次 dry（不写盘）。但它是代码变更，要走审计。
2. **试点 3 章（ch745/650/654）怎么处理？** 建议并入 R251 阶段 1 那一次提交——工具对已修行是跳过的（幂等），三章状态不会被二次改动。

## 八、工作量实话（2026-09-24 实测修订）

R251 阶段 1 + 1b 已把**机械能做的部分做完**（2,286 对，V5 从 3.9 → **10.6/千**）。一·补第 5 条证明再调松规则净增 0 对——**剩下的全是读句子才能判的**，这部分没有捷径。

- **已知缺陷量**：(甲) 引述旁无引号的行 ~954 处（真数约半）/ 180 章；(乙) 可证标点残渣 1,442 处 / 119 章。两项相加 ≈ **1,900–2,400 个待修位置**，摊到 200 章约 **每章 10–12 处**。
- **折算密度**：若这批全部落实，V5 约到 **13–15/千**；要到 20/千 还需要**「无引述结构的纯台词」**（`"嗯。"`、`"走吧。"` 这类，检测器数不到），那部分只能靠通读。所以**第 2 条的目标带 20–28/千 对 V5 未必够得着**，最终落点以通读结果为准，不要为凑指标硬加引号。
- **本轮性质**：R238 是**删**，可用脚本判定；这一轮是**补**，补在哪儿只有读得懂上下文才知道，且**父本 77% 的行对不上**（一·补第 6 条）。10 批、一批一次交付 + 抽读，是全书迄今最大的一轮。
- **不要做的事**：不要为凑密度给强调/术语加引号（那是撤销 R238）；不要回填父本被有意删掉的词（隐喻简化、`目光→视线`、`愣→怔`、`——→，` 都是既定简化）；不要动那 1,823 处**不可证**的 `。`/`，` 差异（多为有意逗号链）。

## 九、进度登记

| 批次 | 章号 | 改动行 | 新增引号对 | 该批密度 | 验收 |
|---|---|---:|---:|---:|---|
| R251 阶段 1 | 551–750 | — | 2,286 | 10.6/千 | 已提交 `b04b2416` |
| **B1a（自然序 1；表内 B4 的标点半批）** | 551–570 | 118（15 章） | 13 | 0.7–13.9/千 | 1/4/5/6/8 过，**7 未达**（见下） |
| **B1b（自然序 1；(甲) 类补引号）** | 551–570 | 69（17 章） | 112 | 6.3 → 9.3/千 | 1/2/3/4/5/6/8 过，**7 未达**（见下） |
| **B2a（自然序 2；标点半批）** | 571–590 | 83（14 章） | 4 | 9.2 → 9.3/千 | 1/2/3/4/5/6/8 过，**7 未达**（见下） |
| **B2b（自然序 2；(甲) 类补引号）** | 571–590 | 96（18 章） | 122 | 9.3 → 12.6/千 | 1/2/3/4/5/6/8 过，**7 未达**（见下） |
| **B3a（自然序 3；标点半批）** | 591–610 | 159（19 章） | 4 | 5.3 → 5.4/千 | 1/2/3/4/5/6/8 过，**7 未达**（见下） |
| **B3b（自然序 3；(甲) 类补引号）** | 591–610 | 114（20 章） | 203 | 5.4 → 11.2/千 | 1/2/3/4/5/6/8 过，**7 未达**（见下） |
| **B4a（自然序 4；标点半批）** | 611–630 | 29（7 章） | 1 | 6.0 → 6.1/千 | 1/2/3/4/5/6/8 过，**7 未达**（见下） |
| **B4b（自然序 4；(甲) 类补引号）** | 611–630 | 69（17 章） | 100 | 6.1 → 9.3/千 | 1/2/3/4/5/6/8 过，**7 未达**（见下） |

**B1a（2026-09-24，提交 `5f6d860f`）** —— 三处必须记下的判读：

1. **拆成 a/b 两半。** 计划 §四 把 B4 当成一个整批，但 §五 第 7 条（密度 18–28/千）**纯靠标点够不着**：本批 118 行只新增 13 对引号。故 B4 拆为 **B1a＝标点修复 + 整行台词**（已提交）与 **B1b＝(甲) 类补引号**（待做，需逐行读上下文）。**B4 作为整批尚未过验收**，第 7、9 条要等 B1b。

2. **(乙) 规则的完整形态已被反证并补全。** 父本 `A。"X"。B` → 现值 `A，XB`，逆变换是**删逗号 + 按父本补句号**。三个反例已单列手工行：`被。"上级"。"终止"了。`（句号收的是相邻强调引号链，不是句子）不补句号；`东西，。"保护欲"。`（逗号是同位语标记）保留逗号；ch563 L111 父本 `。"` 是**真实的叙述/台词分界**，逆变换会把叙述粘进台词，故整行手工。

3. **丁类已量化并留档，未动。** 本批范围内，与父本仍有差异但**规则按计划不许碰**的位点共 **88 处**（父本 `灰色的制服。灰色的脸。空白的编号牌。` → 现值 `灰色的制服，灰色的脸空白的编号牌。` 这类**有意逗号链**，§八 明令勿动），另有 8 行因长度不等未纳入比对。明细存 `.claude/tmp/r252_b1_ding.txt`，供作者判断是否另立一轮。规则**有意处理**的位点是 147 处（父本自身的强调引号灾难被摊平），两者已分开统计。

**工具：** `tools/fix_r252_b1a.py`（apply/verify/dry，整行锚点、唯一命中、fail-closed、幂等）+ `tools/r252_b1_sites.json`（118 行改动清单，含 `core_before`/`core_after` 供审读）。`--verify` 从 HEAD 的 blob 重放全表并要求逐字复现工作区，且校验行尾约定与锚点一致 —— 这是同时证明「只有预期处变了」与「CRLF/LF、无 BOM 未被扰动」的唯一检查。

**勘误一条：** 规划器最初从 `git show HEAD:` 取锚点，但 V5 工作区是 CRLF、blob 是 LF（`.gitattributes eol=lf` 提交时归一化），锚点永远搜不到。锚点必须按**工作区字节**建，比对必须在 **LF 空间**做。

**B1b（2026-09-24，(甲) 类补引号）** —— 69 行 / 17 章 / **新增 112 对引号**。最重的是 ch567：全章 113 行、改动前只有 2 个引号，本批 16 行 / 26 对。本批**不新增任何字数**，只补引号与剥掉的标点。

**判据（四条轴）：**

1. **补引号**：行内**有人名 + 言语动词**（`X说/问/答/开口/打断…`）即为 (甲) 位点。修复形状取本书**自己的**既有规范 —— ch560 L61 本来就有 `"你为什么不回答？"维护派执行者的声音在重叠。"锚点A0428碎片在哪里？"`，即 `"台词"叙述。"台词"`，两段台词各带各自的收尾标点；ch564 L181 `"轩哥？"赵大嘴的声音在0429的信号中。"你看到了什么？"` 同形。补完的 ch564 L79 已与父本 L85 **逐字相同**。
2. **`说：`/`继续说：` 一律不动。** V4 语料（未受本轮影响）同时存在裸文体 `余温说：那些记忆里面的感情…` 与引号体 `它说："小叶，你做了一个很重要的选择。"` —— 两种并存是既定写法，不是剥引号的伤。据此 ch565 L27、ch567 L101、ch569 L41 段、ch570 L129/L139 全部未动。
3. **`「」` 一律不动。** ch564 L115/L161 的 `「选择B路径，伪装为拖油瓶。维持闭环稳定性。」` 是 §一·补 9 认定的标签体（系统指示文本），L161 的裸文版是同一文本的转述，不是缺引号。
4. **不动的三类**：自由间接引语（ch564 L183 `告诉他你看到了什么`）、间接引语（ch565 L73 `但赵大嘴说，从第一次…`）、叙述与台词粘连的合成句（ch567 L43 —— 前半是叶文轩说话，后半 `它在叶文轩的系统终端和赵大嘴的手机之间架了一座桥。` 与同章 L15 的旁白逐字重复，需要改写而非加引号）。**这三类需作者定夺，不是本批遗漏。**

**本批暴露、已量化、按计划未动的三类新伤：**

- **半角撇号族**：ch566 拿 `'` 当引号且**不收尾**（`然后电话那头有人说：'你好，请问是叶文轩先生吗？`）。**全 V5 166 处 / 38 章**，本带只 ch566 的 11 处，其中 3 处台词位点已在 B1b 内改回 `"` 并补上收尾引号；其余 35 章 155 处待另立一轮。
- **强调引号残渣 `静`**：剥引号删了 `"静"` 的引号、把字留下了（`…一点波澜都没有。静，但赵大嘴感到下面的重量。`）。**全 V5 只 4 处，且全在本带**（ch561/569/570）。这是**字**的伤不是标点，§八未授权，未动。同族的 `他清了清喉*` 见 R249 待办。
- **模板化复读块**：ch570 L41/L89/L137 与 ch569 L81/L89 共用同一句旁白（`叶文轩的声音…0429备份的传输质量已经很好，叶文轩能正常说话，赵大嘴能正常听到。`），且句中缺句号（`传荡开来0429备份`）。本批只补台词引号，旁白未动。

**验收：** 1/2/3/4/5/6/8 过 —— CJK 逐章相等（20/20）、≥3000（20/20）、引号偶数、L1+DEADLY 不涨、112 对全为台词（人工逐行读过）、`--verify` 从 HEAD 重放 69 处复现工作区。**第 7 条未达**：本带密度 **6.3 → 9.3/千**（ch567 17.8 最接近）。与 B1a 结论一致 —— **纯补台词也到不了 18–28**；剩余缺口是「无引述结构的纯台词」，只能通读。

**工具（B1b 新增）：** `tools/r252_b1b_sites.json`（69 行清单）+ 底稿 `.claude/tmp/r252_b1b_plan.txt` 与 `.claude/tmp/r252_b1b_build.py`。构建器**不转录正文**，只给切点标记，切点逐字符从文件里取，并断言：切点在行内唯一、首段若为台词必须位于行首（否则会把叙述前缀卷进引号）、`subs` 规则在段内唯一命中。三条断言各拦下一次真实误编辑，全部在写盘前 ABORT。`tools/fix_r252_b1a.py` 增 `--sites`（默认值不变，B1a 调用式不受影响），两表共用同一台 fail-closed 引擎。

**B2a（2026-09-24，提交 `b57750e2`）** —— 标点半批，83 行 / 14 章（ch587 一章占 28 处），只做 §三 授权的第 2 类编辑。

1. **崩坏模型定型。** 强调引号被摊平后写作 `。"X"。`（句号被搬进了新引号对），随后剥离轮跑 `。"`→`，` 并删掉引号字符，于是句子边界从关键词**之后**跑到了**之前**，成了一个逗号。逆变换因此是均匀的：删逗号，并在父本有句号而现值没有时把句号补回关键词之后（乙1／乙2）。已用 B1a 的验证样本（`预置的，记忆提取模块。`→`预置的记忆提取模块。`）与父本形态（`，。"X"。` 同位语保留逗号）双向核对。
2. **本带认证类已清空（反证法）。** 把修好的正文重新喂回真正的规划器 → **提案 0 条**。做法是先把已提交的表备份、跑完再还原并核对 md5（`eef23d8562a2a5cf38d61f25ea849298`）—— 规划器尾部是可执行代码，`import` 它会重跑并覆盖表。
3. **丙类整行台词 4 处**补引号（ch574 L107/L141、ch575 L5、ch585 L68）：父本整行带引号的台词，剥离轮把引号全删了。
4. **三类未动（§八 未授权）。** ①**游移句号 7 处**：后来的某一轮把剥离轮的逗号改回了句号，但句号仍留在关键词**之前**；②**缺失句号 212 处**：父本有 `。` 而现值什么都没有，两句直接粘连（`光晕在慢慢变淡0428碎片的锚点还在`）；③**79/83 行在忽略引号后仍与父本不同，但这不能当"残留伤"引用** —— 其中大部分正是父本自身被制造出来的句号，规则本就要删。三项明细存 `.claude/tmp/r252_b2_residue.txt`。
5. **验收**：1/2/3/4/5/6/8 过（CJK 逐章相等 20/20、≥3000、引号偶数、L1+DEADLY 不涨、83 处 `--verify` 从 HEAD 重放复现工作区）；**第 7 条未达**：本带 9.2 → 9.3/千 —— 标点几乎不动密度，缺口全在台词引号，故紧随其后做 B2b。

**B2b（2026-09-24，提交 `94a8a2cb`）** —— (甲) 类补引号，96 行 / 18 章 / **新增 122 对引号**。检测器本带提出 131 个候选，35 个放掉（96 + 14 + 21 = 131）。

**本批判断规则（比 B1b 更保守，两条硬判据）：** 只有**与归属语相邻的那一段**是"经认证的台词"，加引号；归属语**之后**的句子，仅当文本自证是台词时才加 ——（a）含第一/第二人称代词（`我/我们/你/你们`；本书叙述通篇第三人称，叙述不会对叶文轩称"你"），或（b）是问句（`她是谁？`、`你感到吗？`）。其余一律留在引号外。

- 这条规则当场拦下一处：ch578 L49 的 `她的语气很平淡静，` 是叙述从句，构建器首版把它卷进了台词（`"我信任你父亲，"沈知秋说。"她的语气很平淡静，所以我相信你。…"`），已改成 `"我信任你父亲，"沈知秋说。她的语气很平淡静，"所以我相信你。…"`。**裸尾不是伤，错发的引号才是** —— 这是本批相对 B1b 的方法差异：B1b 对归属语之后的段多数加了引号（`(553,29)`、`(567,35)` 等），B2b 默认不加。差异方向保守，且 39 处裸尾已单列成清单（`.claude/tmp/r252_b2b_residue.txt` 第一节）待作者裁定；作者若认为该加，是在既有行上再插一对引号的一步操作。
- **判据②首次拿到父本证据。** ch571 L179、ch575 L137、ch580 L119 三行 `0428碎片在回廊的第四层中说：…`，其父本**同样写成裸冒号**（父 L313／L153／L123）。剥离轮之前的父本就这么写 —— 裸冒号体是本书自己的写法，不是剥离轮的伤。本带共 30 处 `说：` 行，一律未动，其中 14 个进过候选。
- **21 个候选根本不是台词**：纯叙述碰巧含言语动词（`他摸不着该从哪里开口`、`0428碎片的粉色光在脉动，在等叶文轩回应`），或言语动词落在别的词里（`陈小鱼就会开口`）。
- **本带新量化、未动的两类**：①**裸尾 39 处**（见上）；②**以名词短语作归属语的台词 14 处** —— `赵大嘴的嗓音干得发涩。`、`那里是什么？赵大嘴的声线有些发干。`、`他清楚我会来？叶文轩的声调发涩。` 这类，检测器要求"人名 + 言语动词"故提不出候选，其中 9 处显然是没引号的台词。§八 未授权，未动。
- **半角撇号**：本带仅 ch575 L35 一对（`声音说：'你的女儿还在。…'`），在已改行上顺手改回 `"`；其余 155 处仍待专轮。
- **验收**：1/2/3/4/5/6/8 过（CJK 逐章相等 20/20、≥3000、引号偶数、L1+DEADLY 不涨、96 处 `--verify` 从 HEAD 重放复现工作区）；**第 7 条未达**：本带 9.3 → 12.6/千。章间极不均：ch579 31.2、ch575 26.0、ch572 25.2 已达标，而 ch588 3.3、ch571 4.0、ch582/ch584 5.8 差得远 —— 剩下的正是 §八 说的**「无引述结构的纯台词」**，检测器数不到，只能通读。
- **工具**：`tools/r252_b2b_sites.json`（96 行清单）+ `.claude/tmp/r252_b2b_build.py`（B1b 构建器的副本，只改输出路径与 SPEC，两批的表互不覆盖）。

**B3a（2026-09-24，提交 `cc819216`）** —— 标点半批，159 行 / 19 章（ch596 本带无位点），只做 §三 授权的第 2 类编辑。

1. **崩坏模型连续第三带成立。** 用 B2a 的同一台规划器跑本带（`.claude/tmp/r252_b3_plan.py`，只改输出路径与默认范围）→ **159 条提案、全部以父本为证**，无一条需要新判据。分类：**乙1 121 行（单点）／乙2 31 行（两点）／乙3 3 行（三点）＝ 192 个逗号位点**；另 **丙 4 行整行台词**补引号（`"嗯？"`：ch592 L175、ch598 L173、ch603 L129、ch604 L69）。乙类不新增引号，故本批只 +4 对。
2. **一行按规则处理、留下词级缺陷**：ch609 L99 `等了一个，很多年。` → `等了一个很多年。` —— 逗号可证是剥离轮残渣（父 L107 写 `等了一个。"很多年"。`），但 `一个` 是**词**的伤，§八 未授权，已入待办。
3. **验收**：1/2/3/4/5/6/8 过（CJK 逐章相等 20/20、≥3000 20/20、引号偶数、L1+DEADLY 不涨、159 处 `--verify` 从 HEAD 重放复现工作区）；**第 7 条未达**：本带 5.3 → 5.4/千 —— 与 B1a/B2a 同结论，标点不动密度，缺口全在台词引号，故紧接着做 B3b。验收明细存 `.claude/tmp/_b3a_acc.txt`。
4. **工具**：`tools/r252_b3_sites.json`（159 行）+ 复用 `tools/fix_r252_b1a.py`（引擎未改）。审计 `LEGACY_IMPACT_RESULTS/20260924-33`（`transform`）／`-34`（`main`），gate=PASS、raw_matches=0。

**B3b（2026-09-24，提交 `df79d959`）** —— (甲) 类补引号，114 行 / 20 章 / **新增 203 对引号**（父本推导 58 + 人工裁决 56）。

1. **判据升级为「父本权威」。** B1b/B2b 靠行内自证（人名 + 言语动词）认台词，本批第一次把**父本 `9252b142^` 当作引号位置的唯一权威**：剥离轮吃掉的两类引号对中，「台词」是可证附伤（补回），「强调」（`"关键词"`）是有意为之（永不补回）。由此得四条硬规则：①强调对、半角撇号、`124年`/`121年` 这类**用词差异照现值**；②现值丢掉的标点**不补**（§八）；③父本无对应行（被改写过）的，只引**紧邻归属语之前**的那片台词；④父本整行即台词的按父本补。58 条来自「引号/撇号/逗号盲键」对齐的推导（对齐 70/126），其余 56 条与父本逐行并读后裁决 —— 剥离轮把引号周围的叙述（归属语、尾句）也改写过，位置搬运在那里不可信。
2. **检测器的一处形状缺陷被校正。** `is_dialogue` 的 `NAME = [一-鿿]{2,4}` 匹配不到五字人名 `未来叶文轩`，`未来叶文轩说，` **之前**的台词因此被系统性漏判；本批把该形状放宽到 `{2,5}`，推导数 42 → 58。
3. **本批保留、登记在案的**（明细 `.claude/tmp/r252_b3b_residue.txt`）：**12 处刻意未动**（纯强调行、父本同写裸冒号或行内无台词结构的位置）；**12 处「裸尾」**经与父本逐条对读，确认**父本同位置写的也是叙述**，不是遗漏（与 B2b 那 39 处性质不同）；**2 处缺逗号**（ch598 L107、ch605 L11）按 §八 不补，现值成 `"台词"X说。` —— 该形状全书仅 4 例（对照 `X说，"…"` 1,460 例），作者若判「补」，每行插一个 `，` 即可；**3 处句级受损**（ch597 L104 的尾句 `因为选C答。`、ch608 L141 的叙述/台词粘连、ch609 L13 的直接引语改写残留）须改写而非加引号。半角撇号按规则原样保留（本带 ch597 L60、ch601 L99）。
4. **验收**：1/2/3/4/5/6/8 过（CJK 20/20 逐章相等、≥3000 20/20、引号偶数、L1+DEADLY 不涨、114 处 `--verify` 从 HEAD 重放复现工作区）；**第 7 条未达**：本带 5.4 → 11.2/千。章间不均：ch597 +30、ch599 +19、ch608 +17、ch609 +14 最重，ch596 +1、ch593 +2 最轻 —— 剩余缺口仍是 §八 说的「无引述结构的纯台词」，检测器数不到，只能通读。
5. **工具**：`tools/r252_b3b_sites.json`（114 行）+ `.claude/tmp/r252_b3b_gen.py`（切点标记法：不转录正文，切点逐字符取自文件；断言切点行内唯一、首段若为台词必须居行首、`subs` 唯一命中、CJK 序列不变 —— 本批 dry run 四条断言均未触发 ABORT）。审计 `-35`（`build_line`）／`-36`（`load_derived`）／`-37`（`transform`）／`-38`（`main`），全 gate=PASS、raw_matches=0。
6. **由此提出的后续（待作者裁定）**：「父本权威」既然可行，B1b/B2b 的尾部欠补（父本对照下约 39 处）与 B2b 单列的 39 处裸尾，建议另立一轮**「父本权威回扫 ch551–750」**统一裁一遍，而不是零散补丁。

**B4a（2026-09-24，提交 `a6bef128`）** —— 标点半批，29 行 / 7 章（本带 20 章中 13 章无位点），只做 §三 授权的第 2 类编辑。

1. **崩坏模型连续第四带成立。** 复用 B3a 的同一台规划器（`.claude/tmp/r252_b4_plan.py`，只改输出路径与默认范围）→ **29 条提案，全部以父本 `9252b142^` 为证**，无一条需要新判据，无一条需人工改写。分类里 **丙 1 行整行台词**（ch618 L59：父本整行是一句带引号的台词，剥离轮把引号删光，现值成了裸句）。乙类不新增引号，故本批只 +1 对。
2. **本带可证位点本来就少。** 20 章里 13 章一条提案也没有 —— 不是规划器失灵，而是剥离轮在这一带**大段重写过叙述**，能证出 `。"`→`，` 逆变换的只剩这么几处。缺口全在台词引号，故紧随其后做 B4b。
3. **验收**：1/2/3/4/5/6/8 过（CJK 逐章相等 20/20、≥3000 20/20、引号偶数、L1+DEADLY 不涨、29 处 `--verify` 从 HEAD 重放复现工作区）；**第 7 条未达**：本带 6.0 → 6.1/千 —— 与 B1a/B2a/B3a 同结论，标点不动密度。
4. **工具**：`tools/r252_b4_sites.json`（29 行）+ 复用 `tools/fix_r252_b1a.py`（引擎未改）。审计 `-40`（`plan`）／`-41`（`apply_sites`）／`-42`（`transform`）／`-43`（`main`），全 gate=PASS、raw_matches=0。

**B4b（2026-09-24，提交 `f74253a8`）** —— (甲) 类补引号，69 行 / 17 章 / **新增 100 对引号**（父本推导 3 + 人工裁决 66）。底稿对全带 82 个候选做对齐，只有 3 个对上；落入手工裁决的 79 个里，**13 个放掉**（66 + 13 = 79）。

1. **本带的自动对齐率是全轮最低的，父本改作「人工定位后逐行并读」。** 79 个手工候选里，77 个被判「父本未对齐」，另 2 个另有原因（ch614 L99 行内已有引号、ch625 L37 父本那一对不是台词对）。于是把每个候选都放回父本人工找对应行：**27 处找到并据此定夺**（如 ch629 那串对话、ch630 L23 守门人那段、ch611 L178 的 `"我得走了，"`），其余候选项的父本已被整段重写，只能按行内形状判。四条判据：①父本有可比行的，按父本还原**台词**对、**永不还原强调对**（`"关键词"`）；②父本无可比行的，只引**紧邻归属语之前**的那片；③归属语**之后**的句子沿用 B2b 的保守判据 —— 归属语以 `，` 收尾（话还在继续）**且**该句读起来是台词才加，`声调平得没有起伏。`、`走廊很静。` 这类描写说话人的句子一律留在引号外（ch625 L53、ch629 L33）；④现值丢掉的标点不补（§八）。
2. **13 个放掉的分两类。** ①**6 处 `说：` 裸冒号体**（ch611 L117、ch612 L25、ch613 L119/L129/L131/L133）：判据②，本书自己的转述写法，不是剥离轮的伤；②**7 处不是台词**（ch618 L108、ch619 L79、ch620 L87/L89、ch628 L59、ch629 L13/L19）：纯叙述行碰巧含言语动词，或言语动词落在别的结构里。
3. **构建器本批的两处修订**（都在写盘前拦下真实问题）：①**锚点可向前加长** —— ch616 L45 与 L222 是**同一条句子出现两次**（`好看，叶文轩说。`，父本同样两处），整行锚点不唯一；改为逐行向前扩上下文直到唯一（该两处各扩 2 行），锚点仍是内容而非行号，`--verify` 与行尾校验照常通过。②**行首切点不再要求「行内唯一」** —— ch623 L29 的 `同意，` 在同一行出现两次（行首、`你在意识中说同意，`），但首段切点位于 0 位，左端匹配是唯一读法；现在只对**非行首**切点要求行内唯一。
4. **验收**：1/2/3/4/5/6/8 过（CJK 逐章相等 20/20，61183 → 61183；≥3000 20/20；引号偶数；L1+DEADLY 不涨；69 处 `--verify` 从 HEAD 重放复现工作区）。**第 7 条未达**：本带 6.1 → 9.3/千（B4a+B4b 合计 +100 对）。章间不均：ch629 +27、ch621 +15、ch624 +12、ch625 +10 最重，ch612/ch618/ch619 一章未改 —— 剩余缺口仍是 §八 说的「无引述结构的纯台词」，检测器数不到，只能通读。
5. **工具**：`tools/r252_b4b_sites.json`（69 行）+ `.claude/tmp/r252_b4b_gen.py`，决策表 `.claude/tmp/r252_b4b_manual_spec.txt`（一行一位点，格式 `章|行|父行|('切点','标签')…`，切点逐字符取自文件，不转录正文）。审计 `-46`（`load_derived`）／`-47`（`load_manual`）／`-48`（`build_line`）／`-49`（`make_anchor`）／`-50`（`main`）／`-51`（`transform`）／`-52`（`main`），全 gate=PASS、raw_matches=0。


